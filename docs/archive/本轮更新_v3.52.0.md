# 更新说明 v3.52.0

## 1. 「移出在读」多余按钮——删了 ✅
书详情里那个「移出在读」按钮去掉了。要改状态，点「状态」那一行的按钮就能循环切 **未读 → 在读 → 已读**，不需要重复入口。连它背后没用的函数也一起清了。

## 2. 又完整复查了一遍（功能 vs 你的要求）
19 项逐条在代码里核对，全部在位 / 该删的已删：
总评找回+置顶、书架最新在读排前、划线偏移定位+兜底+就地重绘、前后翻页动画、搜索与筛选分离、多选栏深色、移动端加载全、安装到主屏入口、理清章节存库、Quill 历史、拖动改作者、按正文合并笔名、进度条拖动跳读、种树最新排前、书单移到阅读记录、阅读回顾几遍、移出在读已删、多选栏书单入口已删。
复查结论：**无重复函数、无失效按钮引用、无孤儿函数**，全部编译通过。

## 3. 继续没做完的——整理流水线（这轮推进两步）
- **③ 分章节其实已经有了**：它并在「**完结识别 + 分章节**」这个批量任务里——全库扫一遍就把每本的章节数（chapter_count/番外）和完结状态都存进库了。我把这张卡片名字改清楚，省得你以为没做。单本想补就用详情里的「理清章节」。
- **⑦ 管理面板「由浅入深」重排（初版）**：管理页顶部新增 **「整理流水线 · 推荐顺序」** 导航，1→7 一步步列出来（入库 → 完结+分章节 → 找回作者(书名) → 按正文合并笔名 → 整理作者名 → 去重 → 打标签），**点哪步就跳到对应卡片并高亮**。这样整套整理不再东一个西一个，照着从上往下做即可。

剩下的 ④去重、⑤去重后打标签、⑥版本沿用旧笔记——工具都在，下一轮把它们按这个顺序"串"起来（带预览和备份）。

---
版本号沿用 SemVer：加了功能（管理面板导航）→ 次位 +1 → **v3.52.0**。

升级 v3.52.0：结束 python/pythonw → 启动 → 书房和阅读器各 **Ctrl+Shift+R 刷两次**、阅读器窗口关掉重开。
覆盖建议同上版：解压到新文件夹，把旧的 `config.json` 拷过去最干净。

**先看**：①书详情里「移出在读」是不是没了；②打开「管理」页，顶部有没有「整理流水线·推荐顺序」，点某步会不会跳到对应卡片。


