# 更新说明 v3.54.0（收尾）

## 还有没完成的吗
整理流水线 ①~⑦ 全部到位了：入库 → 完结+分章节 → 找回作者(书名) → 按正文合并笔名 → 整理作者名 → 去重(沿用旧笔记) → 打标签(默认只标没标过的)。管理页顶部「整理流水线·推荐顺序」照着走即可。

## 丢失的总评，能找回吗——这轮给了真正的找回口
- 现在还在库里的简评：去「阅读记录 → 我的总评」就能看到。
- 当年真的没保存上、或后来丢了的：新增「**找回丢失的简评（从备份）**」（管理页）。它扫数据库的历次备份（library.db.bak*），把"备份里写过、但现在库里这本是空的"简评挑出来，先预览、确认后写回，**不会覆盖你现在已有的**。逻辑已单测。
- 实话：能找回多少取决于有没有对应时间点的备份；如果当时既没保存成功、也没有备份，那就没有副本了。

## 你问的几个，复查结果
- 移动端下载到主屏：齐全。安装入口默认显示，点了按 iOS/安卓/电脑给步骤；PWA 的 manifest 和 service worker 都有，安卓可直接装、iOS 用"添加到主屏幕"。
- 翻页：修好了。真实翻页(StPageFlip)有动画，且加了"翻不动就强制翻"的兜底；长篇/窄屏的回退单页翻，前后翻都有滑入动画。
- 划线挂不上：上版已加固（选区找不到位置块时改在正文里就近定位，不再乱猜）。

## 清理
又删了 3 个没用到的旧函数(renderBookmarks/renderHighlights/setAmbVol)。全功能复查 18 项全过，无重复函数、无失效引用、无孤儿。

---
v3.54.0：升级=结束 python/pythonw → 启动 → 书房和阅读器各 Ctrl+Shift+R 刷两次、阅读器关掉重开。覆盖建议：解压到新文件夹、把旧 config.json 拷过去最干净。
先试：管理页「找回丢失的简评」点「扫描可找回的」，看能不能从备份里翻出你丢的那些。
