# 更新说明 v3.64.5

## 卷曲翻页——这次真让它自己回来（不用你放任何文件）
你说得对：jsdelivr 那个链接你这边能打开，库是拿得到的；之前没生效是我把脚本设成 defer、加载晚于初始化，等于白拿。这版加了个"**等库到货**"的轮询：阅读器开页后，库一旦从 jsdelivr 加载好（通常一两秒内），就**自动升级成卷曲翻页**，不用你手动放任何文件。
- 联网（能连 jsdelivr/unpkg）→ 卷曲翻页自动回来；
- 完全离线又没放本地库 → 用内置翻页（这版也更柔和了）。
我之前把它停掉是误判（以为它导致两页，其实是缓存旧码），现在恢复了，并保留"万一变两页就自动退回单页"的保险。

## 双击作者名 → 真·全部作品（不再只在当前视图里筛）
之前的毛病：在「收藏」里双击就只在收藏里筛、在书库里就只在书库里筛。现在双击会**先切回完整书库、清掉收藏/题材筛选、按作者分组**，再全库找这位作者 → 她的作品真正堆一起。书房作者那排、作品详情里的作者名，双击都这样。

## 作品详情：去掉多余的「改」
作者名双击=跳转；那个多余的「✎改」去掉了。（要改作者名，编辑模式里把书拖到目标作者那排即可。）

---
v3.64.5。升级：覆盖进 `D:\MyLibrary_v3`（留 config.json）→ 结束 python/pythonw → 启动 → 阅读器窗口关掉重开 → 书房 Ctrl+Shift+R 刷两次。


