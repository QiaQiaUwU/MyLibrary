# 本轮更新（第 28 批）

## 1. 去重崩溃修复（重点）
扫描重复时报 `NameError: name 'n_hash' is not defined`（mylib_admin.py 第 277 行）——
统计第一层 content_hash 副本数的变量漏写了。已补上，「扫描重复 / 执行去重」可以正常跑了。

## 2. 标签库可以删
在「编辑书籍信息」里，下面那排标签库现在每个词鼠标悬停会出现一个 **×**：
- 自创的标签：点 × 直接从库里删掉。
- 内置的标签：点 × 后记进"隐藏表"，以后不再作为建议出现。
（这些都存在你本机浏览器里。重新输入同名标签会让它再次出现。）

## 3. 标签库可以拖动分类
打开「按标签筛选」后，**把任意标签拖到别的维度行**（性向/CP、世界观/背景、设定/套路…）即可重新归类，
松手后目标维度会自动展开让你看到。归类结果存在本机浏览器，下次打开还在。
（拖动＝换维度；单击标签还是原来的 必含→排雷→取消。）

## 关于上一批点 4
你确认收藏的书确实能删（详情弹窗点「已收藏」取消即可），这点不用再改。
剩下两项仍在排队：番外更新提醒、补全字数/章节元数据——告诉我先做哪个。

> 提醒：PWA/Service Worker 有缓存，更新后请按 **Ctrl+Shift+R 刷新两次**。

