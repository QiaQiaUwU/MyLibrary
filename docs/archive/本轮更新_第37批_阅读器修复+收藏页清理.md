# 本轮更新（第 37 批）

## 这批已修（你截图里点 2、3、4 的可立刻做的部分）

### 阅读器（点 4）
- **翻页修好了**：之前靠 click 事件，StPageFlip 会把书页上的 click 吞掉。改成用 pointerdown/pointerup 自己判断"轻点"——左 28% 上一页、右 28% 下一页、中间呼出工具栏；有明显移动（在选字划线）就放过去、不翻页。
- **声音小窗关不了 → 修好**：原来点×会先 closeAll 清掉再 toggle 又加回来，等于永远关不掉。改成：本来开着就直接关。
- **书签 + 划线笔记 合并**：列表抽屉现在是「目录 / 书签·划线笔记」两个页签，书签和划线、笔记按在书里的位置混排在一个列表里。
- 声音小窗排版：限制在窗口内、可滚动，"全部停止"改"静音"更紧凑（上批已做）。

### 收藏页（原"手账本"，点 2、3）
- **去掉封面上的"在读 X%"标志**（点 2）：状态在详情弹窗里可改，不再在封面重复。
- **"收藏 · 手账本" → "收藏"**（点 3）：去掉"手账本"几个字；页面标题也改成"收藏"。
- **去掉上面的"翻开 ›"**（点 3）。
- **已读的书不再显示"读到5%"**（点 3）：标了"已读"就显示"读完"（有日期就带上日期），只标"开始读 / 读完"，**不再显示"最近"**。
- **双击句子跳到原文**（点 3）：收藏页里的句子，双击就跳进小说对应位置（"读原文"按钮也保留）。

## 还没做、需要单独排期的（点 1、3、5）
- **点 1（省略"在应用中打开"）**：那个"打开 MyLibrary 应用"是 Edge 自己的提示，只有在浏览器标签里打开网址时才弹。**用桌面快捷方式启动就不会弹**（直接独立窗口）。这是浏览器行为，App 这边没法直接去掉那一步。
- **点 3：收藏页"像纸质书两页摊开"的翻页效果** —— 需要重做这块 UI，单独做。
- **点 3：收藏页内容改成"按时间排序的、读过的书 + 句子 + 笔记的全记录"（不再是所有收藏的书）** —— 需要改后端取数逻辑，单独做。
- **点 5：整理流水线** —— 这是个大工程，我按你说的顺序排：①判断完结 → ②确认作者（含"读文章开头找作者"）→ ③分章节/番外 → ④去重 → ⑤去重后再打标签（省 token）→ ⑥版本更新沿用旧书的标签/信息/你的笔记。下一批我从这条线开始做。

> 升级 v40：先在任务管理器结束 python/pythonw，再启动；Ctrl+Shift+R 刷新两次；阅读器窗口也关掉重开。

