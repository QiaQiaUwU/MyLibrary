# 本轮更新（第 38 批）

## 这批已做（你这次的两个修正 + 一个新功能）

### 收藏/封面状态标记（修正上一批的过度删除）
- 不是一刀切去掉进度了：**已读完 → 标"读完"**（淡金色），**在读 → 标"在读 X%"**（绿色），未读不标。
- 收藏页（原手账本）的阅读里程同理：已读完只显示"开始读 / 读完"，在读显示"读到 X%"，都不再显示"最近"。

### 阅读时改小说信息（双击标题）
- **详情页**：双击**书名**改书名、双击**作者**改作者（即时写库、书架同步刷新）。
- **阅读器里**：双击顶部的书名，也能直接改书名/作者，不用退回书房。

### 阅读器老问题（上一批已修，这里复述确认）
- 翻页：改用 pointerdown/up 判断轻点（左 28% 上一页、右 28% 下一页、中间呼工具栏；选字不翻页）。
- 声音小窗：能关掉了（之前点×又被加回来）。
- 书签 + 划线笔记：合并成一个「书签·划线笔记」列表，按在书里的位置排。
- 声音小窗排版：限制在窗口内、可滚动。
> 这些是代码层面修好并通过语法校验的；翻页/关闭这类要你在真机上点一下确认手感，若还有不对告诉我。

## 接下来（你点名的大块，我按这个顺序做）
1. **整理流水线 + 管理面板重新设计**（下一批专门做）：把管理页重排成"由浅入深"的有序步骤——
   ①判断完结 → ②确认作者（含"读文章开头找作者"，把散在匿名里的归位）→ ③分章节/番外 →
   ④去重 → ⑤去重后再打标签（省 token）→ ⑥版本更新沿用旧书的标签/信息/笔记；排版也一起重做。
2. **移动端排版适配**（再下一批）。

> 升级 v41：先结束 python/pythonw 再启动；Ctrl+Shift+R 刷两次；阅读器窗口关掉重开。


