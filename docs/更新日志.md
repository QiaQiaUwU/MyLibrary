# 更新日志（新→旧）

## v4.6.19（.gitignore 漏了几个日志文件 + 揪出一个真的该删的老文件）
- **`build_log.txt`/`mylib_log.txt` 不该被追踪**：这两个是跑起来之后本地自动生成的运行日志，不是源码。`.gitignore` 里本来就有 `*.log`，但这两个文件是 `.txt` 结尾，那条规则挡不住，这次单独把它们（还有 `.deps_ok` 这个依赖检查标记文件）加了进去。不过它们俩之前已经被提交过一次了（大概率是最早那次提交时手误带上的），光改 `.gitignore` 对已经追踪的文件不起效，还需要手动 `git rm --cached` 一下才能真正从仓库里摘除，见下面步骤。
- **`frontend/home/home.js` 是真的该删的老文件**：查了一下，这是 v4.2 模块化拆分之前的老版本单文件，现在的代码用的是 `frontend/home/js/` 下面拆好的一堆模块文件，`home.js` 这个单文件早就没用了，但因为解压覆盖升级的方式只会覆盖/新增文件、不会删掉新版本里已经不存在的旧文件，它就一直躺在本地没人管，这次确认了当前版本确实完全用不到，可以放心删。
- **其他几个看着可疑但其实是正常文件**：`mylib/agents/mylib_agent.py` 是另一个独立的"阅读助手"agent（追踪角色/前情提要用的，跟 Quill 是两回事），`frontend/settings/`、`frontend/admin/` 都有真实路由在用，`Dockerfile`、`安卓一键部署.sh` 都是这几轮加的合法新文件——这几个不用动。
- **给了一遍具体的清理命令**（这轮没法在这边跑 Windows 环境验证，命令本身是标准 git 操作）：
  ```
  rmdir /s /q MyLibrary
  git rm --cached build_log.txt
  git rm --cached mylib_log.txt
  git rm frontend/home/home.js
  git add -A
  git commit -m "cleanup stale files"
  git push
  ```
- **测试**：纯 `.gitignore`/文档改动，冒烟维持 36 项不变。

## v4.6.18（bat 脚本乱码修好了 + git 流程不用再绕弯子）
- **`update_github.bat` 报"不是内部命令"，找到真原因了**：查了项目里其他能正常跑的 `.bat` 文件（`启动.bat`、`创建桌面快捷方式.bat`），发现它们全部是纯 ASCII 英文，没有一个用中文+UTF-8——这是项目自己已经踩过、验证过的坑。我加的两个新脚本反而用了 UTF-8 编码+`chcp 65001`+中文提示语，这个组合在某些 Windows 机器/控制台配置下会解析出错（截图里"'??什么？'""'上面的提示。'"这些乱码片段，就是脚本里 `if errorlevel 1 ( 中文内容 )` 这种带括号多行块里的中文被 cmd 错误断句了，是 Windows 批处理一个老issue）。这次把两个脚本全部改成纯英文提示、去掉 `chcp`、去掉容易出问题的括号多行块结构，跟项目里其他 `.bat` 用同一套写法。
- **"为什么要拉一个过时版本下来"——你说得对，不需要**：上一版指南是"先 clone 旧仓库，再把新文件盖上去"，确实绕了远路。改成直接在你现在这个已经是最新版的文件夹里 `git init`，加 `remote`，`push --force` 直接把本地这份覆盖上去——不用先下载一份根本用不上的旧代码。
- **测试**：这轮是纯脚本/文档改动，冒烟维持 36 项不变；两个 `.bat` 文件用 `file` 命令确认过是 ASCII text（不是之前那种 UTF-8 text），逻辑没法在这边跑 Windows 环境测，但控制流已经改成不依赖任何编码敏感结构。

## v4.6.17（疑惑触发逻辑改对了 + 人设不再端着）
- **疑惑那个触发逻辑上一轮搞反了**：你说得对——疑惑该是 Quill 自己没弄明白，不是用户说自己没看懂。之前的写法是用户说"看不懂"就让光点跟着演疑惑，这完全是两件事：用户的困惑，该体现在 Quill 更耐心地解释这件事本身上（回复内容该做的事），不该反过来在光点上演"我也很疑惑"。这次改成扫 **Quill 自己的回复**里有没有"不太确定/拿不准/我也不知道"这类措辞——这才是"Quill 自己没弄明白"在对话里留下的真实痕迹，逻辑上说得通了。
- **人设不再端着**：把"话不多但恰到好处"这句去掉了——这个框定本身就在鼓励惜字如金，容易让回复显得拘谨；"不刻意卖萌或强调自己的身份"也改了措辞，避免连带着把自然的俏皮劲儿也一起压掉。核心身份里现在明确写了"可以有轻松、随口的时分，恰当的时候开个玩笑、俏皮一句都可以，不用总是那么正经"——这条不挂在某个口吻预设下面，是所有口吻（除了本来就要求干练的"简洁"）都带着的底色，不用切到"俏皮"模式才允许 Quill 有点人味儿。老实说这类调优没法保证跟你现在这样聊天的语气完全对齐（说到底还是提示词层面的引导），但方向是对的——把"限制"往回收，而不是继续加"允许"的补丁。
- **测试**：新增疑惑触发逻辑的回归钉子（确保扫的是 Quill 自己的话，不会退回上一轮的错误逻辑）、人设允许开玩笑的检查，冒烟维持 36 项（这轮改动主要在 Python 提示词），pytest 57 → 59。

## v4.6.16（伤心改成下撇嘴+落泪、"在意你"换色、疑惑终于有了正当理由）
- **伤心动画重做**：之前是整体压扁+左右晃+掉一滴通用水滴，这次改成"下撇嘴"——两颗珠子往外下垂，形成一个跟开心的笑弧刚好反过来的倒扣弧线，然后泪珠从嘴角低垂那个点鼓起、拉长下坠、消失，不再是一滴凭空出现的水滴，是从"哭出来的表情"里长出来的。
- **"在意你"的颜色不跟待机撞车了**：算了一下色相才发现，之前的暖棕（#d9b98a）和待机的暗琥珀（#e0b64f）色相只差 7°，本质上是同一个黄橙色系，只是深浅不同——这就是分不清的根源。换成暖赤陶色（#c9836a，色相差了快 30°），保持"在意你"该有的暖调子，但不会再跟待机灯混在一起。这次顺手加了一条冒烟测试，专门算色相差值，以后再有哪个情绪颜色不小心跟别的撞了车能直接测出来。
- **疑惑终于有了正当理由，不是我瞎编的触发点**：你发的那份《人格演变真实性规范》我认真看了，核心是"每一次人格/情绪的可见变化都要能在故事里找到对应的触发事件，不能凭空跳变"。照这个原则，疑惑不该随便挂在一个 UI 点击次数上（那和"发生了什么"完全没关系）——改成从聊天内容本身找依据：用户自己说"看不懂/什么意思/搞不清楚"这类话，才会触发疑惑，这是一个真实存在于对话里、摆在那儿的理由，不是我这边随手指定的。这条判定跟"沉重话题"那套逻辑并列，但性质不同：疑惑是"这一刻"的即时反应（说完就触发一下），沉重是"这段时间"的背景压低（12 分钟冷却窗口内压住外部触发的欢快反应）——分寸感照文档里"演变速度须与事件的重量匹配"来把握，一句话的困惑不该引发什么持续的情绪状态，触发完就完了。
- **为什么没有做成"每个话题都换个情绪基调"那种更大的系统**：文档里也写了"轻微事件不引发根本性人格改变"——如果聊什么话题光点都要跟着换一次颜色，一来大部分日常对话根本没有那么强的情绪色彩可提取（关键词命中法在这种粒度上会经常瞎猜），二来太频繁的情绪波动本身就违反了文档说的"克制"。这次先把明确、可判定、有单一触发点的疑惑补上，其余的暂时不动——如果之后发现某个具体场景也想要类似的"从聊天内容触发"效果，可以照这次的思路继续加，但不想一次性做一个不精确的大杂烩。
- **测试**：新增疑惑触发的关键词验证（含"不该误触发"的反例）、care/standby 色相差值验证，34 → 36 项。

## v4.6.15（先纠正一个错：上轮说错了 + 呼吸感真的加强了）
- **纠正一个错误信息**：上一轮回复里我说"开心=橙色、点一下=开心"——这是错的，很抱歉。重新查代码确认：点一下情绪光点触发的是**害羞**（浅粉 #f7b8cc），连点 4 次以上才触发**开心**（粉红 #e8547e，转一圈），这个映射其实已经在代码里了，颜色也一直是粉色系，不是橙色。上轮给错了信息，这轮已经用一条冒烟测试把这个映射钉死，以后不会再凭印象说错。
- **真正需要动的是这几处，这轮改了**：① 平时呼吸感之前只有光晕在若隐若现地模糊变化（drop-shadow 的虚化半径），没有实际的大小起伏，很容易被忽略——这次加了明显的缩放呼吸（1 → 1.14 倍），跟光晕叠在一起，"在呼吸"这件事现在看得出来了；② 开心的动画之前只在最开始有一下快速的挤压，然后有很长一段（约 2 秒）都是静止悬停在微笑弧形状不动——这次改成开头有 4 次交替的快速挤压/拉伸，才进入分裂-微笑弧-旋转-合拢，读起来更像"呼吸急促"再到"笑出来"，不是一下之后就没了；③ 害羞的呼吸周期从 0.95 秒拉长到 1.3 秒，跟开心的快节奏拉开明显反差，"慢慢地"这个感觉更突出了。
- **顺带说一句"疑惑"这个情绪状态**：歪头冒问号那个动画还在，颜色（淡紫）和关键帧都保留着，但它原本"连点 4 次触发"的入口被这轮的新映射占用了（连点 4 次现在是开心），目前没有任何交互会触发疑惑状态——如果你还想留着这个效果，需要告诉我挂到哪个新触发点上，不然它会一直闲置着。
- **测试**：冒烟新增单戳/连戳的映射回归锁定，33 → 34 项。

## v4.6.14（API 报错加固 + 色卡保存不再静默失败）
- **Gemini 那条"function call 顺序不对"的报错**：查了消息拼装逻辑，本身的顺序是对的（工具调用紧跟用户发言、工具结果紧跟工具调用），但把供应商返回的原始消息对象整个原样回传给下一轮请求时，带上了 `refusal`/`annotations`/供应商私有 id 这类不属于 API 规范的额外字段——这次改成只保留 `role`/`content`/`tool_calls` 三个规范字段再回传，历史消息也顺手只留 `role`/`content`（数据库读出来的 `id`/`starred`/`created_at` 之前也混进去发给模型了）。这类问题在不同中转代理上表现不一样，没法保证这次改完在你朋友那个 Gemini 中转上一定不再报错，但从"该规范的地方规范"的角度这一步是对的。
- **IndexError 那次没能顺着截图定位到具体是哪一行**——查了塔罗、八字、记忆/心事注入这些逻辑，能想到的风险点都已经包了 try/except，没找到明确的漏网之处。但发现一个更根本的问题：聊天接口崩溃时，之前只把异常类型和消息带回前端，服务端控制台什么都没留，等于每次出问题都是"知道炸了，不知道炸在哪"。这次把完整堆栈打进服务端控制台——下次再出现任何崩溃，把服务端那个黑框里的报错截图发我，就能直接定位到具体是哪个函数第几行，不用再靠猜。
- **图片取色的书脊配色应用不上、刷新也没用**——找到一个真嫌疑：色卡保存函数 `saveBookPalettes()` 是"发出去就不管"的写法，保存失败时完全不提示、控制台也没有痕迹——如果保存没成功，色卡在当前页面看着是应用了（内存里已经改了），但刷新一读还是服务端那份旧数据，表现就是"应用不了、刷新也不行"。这和 v4.6.1 修过的阅读设置保存失败是同一个模式（连用的都是同一个 `reading_prefs` 存储），当时只补了阅读设置那一处，色卡这条独立的保存路径漏掉了，这次补上——保存失败现在会有 toast 提示，不再悄无声息。如果你朋友那边是比较早的安装包（GitHub 仓库之前停在 v3.69.1，很可能装的也是那个年代的版本），这个问题本身在 v4.6.1 就已经从根上解决过一次（数据库迁移那个大坑），这次是把**同一类问题**在色卡这条独立路径上也补齐。
- **测试**：新增 `test_v4614_features.py`（工具调用消息清洗、崩溃时堆栈确实打进日志、色卡保存确实不再静默失败），冒烟维持 33 项不变。

## v4.6.13（README 没跟上，这轮补齐）
- **没有好好更新，查了一遍确实有几处过时的**：① "质量闸门"那段测试数字停在"38 项 pytest / 26 项冒烟"，实际早就是 54/33 了——这句话的具体措辞跟我平时批量替换计数用的那几个固定句式不一样，之前每轮改版本号的脚本都没匹配上它，一直没跟着更新，这次改对了；② 塔罗那句"抽完发给 Quill 结合黄历八字解读"是 v4.6.5 之前的老说法——塔罗早就改成结合星座、抽完也不再自动发送而是填进输入框，README 没跟着改，这次改成了实际行为；③ PDF 能拖进库、能在阅读器里读（浏览器自带查看器），README 里一个字都没提过，这次补上了；④ 撤回重编辑、对回复不满意重新生成这两个功能也一直没写进去，补了一句。
- **测试**：纯文档改动，冒烟维持 33 项不变。

## v4.6.12（双击更新 GitHub 的两个脚本）
- **"有没有办法自动更新"——git 做不到真·自动同步（不是网盘），但做到"双击一下"这个程度了**：加了两个 `.bat` 脚本放在项目根目录。`update_github.bat`：日常更新用，双击后自动 `git add + commit + push`，会问一句"这次改了什么"，直接回车也行（用默认说明）。`release_github.bat`：正式发新版本用，双击后输个版本号，自动打标签推上去，会触发上一轮加的自动编译+自动发行版流程，不用再守着等编译完手动传 exe。
- **GITHUB_SETUP.md 也简化重写了**：删掉了之前解释"上一轮为什么写错了"的部分（那件事翻篇了），直接给最短路径——首次 clone，以后双击哪个脚本、什么时候双击哪个，就这几句话。

## v4.6.11（GitHub 指南推倒重写：仓库其实一直都在 + exe 发行自动化）
- **上一轮的 GitHub 指南是按"你还没建仓库"写的，错了**：你发截图之前，我这边完全不知道 `QiaQiaUwU/MyLibrary` 这个仓库存在——压缩后的对话摘要里没提过这件事，属于我这边看不到的历史信息，不是瞎猜，是真没这段上下文。看到截图之后核对了一下：仓库一直都在，只是停在 v3.69.1 没跟上后面的 v4.x 进度。指南已经按"更新一个已存在的仓库"重写，不再是"从零建仓库"那一套。
- **exe 发行版这次做成自动的了**：仓库里原来就有一个 `.github/workflows/build-windows-exe.yml`，但只能手动点按钮触发编译，编完还要自己下载编译产物、自己去网页建 Release、自己把 exe 文件传上去——v3.69.1 那个 Release 大概率就是这么一步步手动弄出来的。这次改成：**推一个 `v` 开头的标签（比如 `v4.6.11`）会自动触发 Windows 编译，编完直接建好 Release、把 exe 挂上去**，不用再手动点按钮、手动下载、手动上传。手动点"Run workflow"的老用法还留着（只出编译产物，不会创建 Release），适合只想测试编译能不能过、不想正式发布的场合。YAML 语法验证过，用的是 GitHub 官方 Windows 跑机自带的 `gh` CLI，不需要额外配置权限密钥（`GH_TOKEN` 用的是 Actions 自动提供的 `github.token`）。
- **测试**：这轮是 CI 配置和文档改动，不适合写自动化测试（说到底得真的推一次 tag 到 GitHub 上才能验证工作流是否真的按预期跑通），YAML 语法和触发条件解析过、结构核对过；真正的验证要等你实际打 tag 推送之后看 Actions 页面的运行结果，如果哪一步报错把日志发我。

## v4.6.10（那条"删不掉"的滚动条真凶找到了 + GitHub 上传准备）
- **"右边那个拉动条真的没法删掉吗"——找到真凶了，不是塔罗面板的问题**：仔细看了你截图里滚动条的位置——它在整个窗口的最右边缘，不是塔罗弹层自己的边缘。查了全部代码，发现**没有任何一处**在弹层打开时把背后的书架页面锁住不让滚——弹层一开，背景页面照样可以滚动，它自己的滚动条也照样显示着，看起来就像"弹层里有个删不掉的滚动条"，其实是背景页面的，跟塔罗那个弹层的排版没关系，之前调整面板大小、缩小卡牌都是在修错的地方。
  这次用一个统一的观察者机制处理：只要塔罗/节气卡/日历/勋章/表情包批量导入/小游戏/每日一签这几个弹层里**任意一个**开着，就把背后页面锁住；全部关掉才放开；多个弹层叠着开也不会关一个就提前解锁。用真实的开关组合测过（开一个/叠两个/关一个/全关四种情况），行为都对。
  单张塔罗那个"今日指引"模式本身内容很少（就一张卡的坑位），不该需要滚动——这次终于是从根上把不该出现的滚动条摘掉了，而不是继续加大面板"绕过"这个问题。
- **GitHub 上传准备**：我这边沙盒没有网络，没法替你直接推送，所以做了两件我能做的事：① 核对了项目自带的 `.gitignore`，补上了最要紧但之前漏掉的一条——`config.json`（里面是你的真实 AI API Key）之前完全没被排除，一旦提交就会把密钥传上 GitHub；② 顺手发现原来的 `.gitignore` 里有两条规则写得不够精确（`_ambient/`、`_quill_stickers/` 没加根路径限定），会连累项目自带的环境音素材（咖啡馆、壁炉、雨声）被一起误伤排除，已经改成锚定到项目根目录，不会误伤 `static/` 下的正牌资源。用假密钥在沙盒里实测过一遍：117 个该进仓库的文件一个不少，`config.json` 确实被挡在外面。另外新增了 `GITHUB_SETUP.md`，从建仓库到推送、以后怎么更新、中文文件名显示乱码怎么办，一步步写清楚了，跟着做就行。
- **测试**：冒烟新增弹层滚动锁的四种开关组合验证，32 → 33 项（这轮没有新增需要 fastapi 的 pytest，`.gitignore`/文档类改动不适合写自动化测试，靠沙盒里的真实 git 操作手动验证过）。

## v4.6.9（塔罗回复不许再编一次额外抽牌）
- **你重发的那张截图里还有一个没处理过的问题**：那张图是 v4.6.8 修复前的旧图（"很高兴能陪你一起读书"那处上一轮已经改了），但仔细看发现 Quill 的回复里还编了一句"我刚刚也为你静心抽牌，指针落在了'魔术师'上"——用户明明只抽了一张「倒吊人」，这张"魔术师"是凭空编出来的第二次抽牌，工具根本没返回过这张牌。之前"不要自己编"这条指令管的是"牌意不要瞎编"，没堵上"编一次额外的抽牌情节"这个口子——这次补了一句明确禁止："只解读用户实际抽到的这几张牌，不要编'我刚刚也抽了一张/指针落在了哪张牌'这种额外情节，工具没返回的牌不存在"。
- **测试**：新增一条检查这条禁止指令确实写进了塔罗那段提示词，pytest 53 → 54（冒烟本轮没有前端改动，维持 32 项）。

## v4.6.8（月相皮肤修对了 + "别刻板"补了具体反例）
- **理解错了——月相是要做 Quill 皮肤，不是塔罗背景装饰**：上一轮把月相图片理解成"塔罗魔法阵的装饰"，你说明白了才发现——`QUILL_AVATARS` 里其实早就有一个"月相"皮肤选项（`moon:{name:'月相'...}`，会显示今天的真实月相），只是形状画得比较将就。这次真正動手改的是**这个已有的月相皮肤**：原来的画法是手写椭圆弧的 sweep-flag（弧线方向标志），容易出错还不好核对；换成跟塔罗那圈月相环同一套稳妥画法——满月轮廓减去一个偏移的"阴影圆"，用反向裁剪区裁出干净的月牙/凸月形状，不用猜弧线方向对不对。
  顺手还揪出一个真的算错的 bug：原来的公式**上弦月和下弦月会画成同一个形状**（阴影偏移量算的时候没考虑"渐盈"和"渐亏"要往两个相反方向偏），比如今天如果是下弦月，图标却可能长得跟上弦月一模一样，不会镜像。这次修成阴影按上弦/下弦分别往左/右偏，验证过两者关于中心对称、真的是镜像形状。月相本身的计算（今天到底是第几天的月相）这次没动——那段公式（已知新月锚点+朔望月周期 29.530588853 天）本来就是天文上常用的准确算法，一直是对的，问题只出在"画出来"这一步。
- **"刻板"这个反馈补了具体反例**：你说的"很高兴能陪你读书"这种明明前面没聊这个话题却硬接上的客套话，我去核对过——不是我代码里写死的模板（搜了整个项目没有这句话），是接的那个模型自己在**工具调用后**习惯性加的"确认+暖场+反问"三件套。上一轮加的"别刻板"提醒比较抽象，这次把你这个具体例子直接写进提示词——人设开头和结尾提醒两处都加了"记生日就好，不要接'很高兴陪你读书'这种没聊过的场面话"，两头卡位（还是那个 U 型注意力的道理：光说"别刻板"这种原则性的话，模型未必知道具体要避免什么；给一个真实踩过的反例，模型才有东西可以对照）。这类调优没法保证 100% 不再出现（说到底还是提示词层面的引导，不是硬规则），但应该能明显减少。
- **测试**：新增月相皮肤的新月/满月特判 + 上弦下弦镜像验证（冒烟 31→32），新增人设反例确实写进了两处提示词的检查（pytest 52→53）。

## v4.6.7（聊天不再刻板：借鉴酒馆的关键词触发 + 塔罗魔法阵加了月相环 + 凯尔特十字再瘦身）
- **Quill 聊天变刻板的真问题，以及怎么参考酒馆解决的**：上一轮给玄学话题（塔罗/八字/紫微/黄历）加了很严格的表格模板，但那段"聊到 X 就必须表格化"的规则**之前是不管聊什么都会塞进系统提示**——哪怕在聊读书心得、闲聊天气，AI 的上下文里也背着一整套"遇到关键词就要走流程"的规矩，容易让整体说话方式都沾上"填表"的僵硬感。这次照着你发的酒馆世界书教程里"关键词触发注入"（world info scan）那一套逻辑改：这段格式规则现在**只在当前消息或最近几轮聊天里出现塔罗/八字/星座/运势这类关键词时才会拼进上下文**，平时聊天的系统提示精简了不少（实测普通聊天场景从约 1300 字符降到 820 字符），不再背着一堆用不上的格式规矩。另外在核心人设里加了一条"别用固定句式开头/收尾，别是先…再…最后…这种走流程的模板腔"，并且在系统提示的**最后一句**（最靠近你实际发的话、注意力权重最高的位置——这也是那几个教程反复强调的"U型注意力"原理）又重复提醒了一遍，两头卡位，不会被中间一大堆规则内容稀释掉。
- **凯尔特十字牌阵再瘦身**：上一轮虽然加大了面板、缩小了摆法，但从你截图看某些窗口尺寸下还是会出现滚动条、根基那张牌被裁切。这次牌和间距又往小调了一档（56px 卡宽，行距/纵列间距也收紧），实测能在 900px 高的窗口内完全不用滚动就看全整个牌阵（此前只能撑到 1080p 窗口）；再矮的窗口（800px 以下）还是会有滚动兜底，这个是数值算过的，不是没做。
- **塔罗魔法阵加了月相环**：照你发的那张月相图片，在魔法阵外圈加了一圈 12 颗月相图标，从新月到满月再到残月一圈循环，用两个圆叠加裁切画出干净的月牙轮廓（比之前那个光秃秃的椭圆+五角星更明显、更有仪式感），几何算过保证新月全暗、满月全亮、中间过渡对称平滑。
- **悬浮气泡的"点开看"提示语去掉了**：按你的要求删掉了这句话，气泡现在只显示连续阅读天数（没有连读记录就显示节气/节日名字），点击的入口还在（鼠标样式和气泡本身的按钮感还在），只是不再有文字提示"点开看"。
- **关于截图里那个"从该页面下载音频"的浮条**：这个不是我这边的界面，是**浏览器扩展**（类似"页面音频下载"这类插件）检测到页面在放音频（塔罗那个背景乐）之后自己弹出来的下载提示条，不属于 MyLibrary 的任何代码——这个我这边改不了，如果不想看到，可以在浏览器的扩展管理里把那个音频下载类的插件关掉，或者对这个网页单独禁用它。
- **测试**：新增 `test_v467_features.py`（玄学关键词触发的判定逻辑、真实跑一遍 `run_quill` 确认两种场景的 system prompt 确实不一样长、月相环的两圆重叠面积算出来的单调对称性、月相环 CSS 优先级），冒烟维持 31 项不变（这轮改动都在后端提示词和纯视觉 CSS，冒烟原有覆盖已经够用）。

## v4.6.6（塔罗改用星座 + 玄学回复按固定模板走，不再随便发挥）
- **塔罗改成星座，不是生日**：你说得对，塔罗是西方占卜传统，用中式生辰八字接不上；上一轮我图省事直接塞了"生日"两个字，这轮改成真正算出西方十二星座（按公历生日的月/日换算，边界日期逐一核对过），系统提示里明确写清楚"塔罗认星座、八字/紫微/黄历认生辰干支，两套体系不混用"。
- **玄学类回复不再让 AI 自由发挥，改成固定模板**：之前的指令比较笼统（"结合干支解读、不超过200字"），这次把塔罗/八字/紫微/黄历/每日一签五种话题**各自的输出结构写死**：
  - 塔罗：先一张表格「位置｜牌名｜正逆｜关键含义」逐张列全，再两三句串成故事；
  - 八字：先一张表格「柱｜天干｜地支｜十神」四柱各一行，再结合五行给性格/近期提醒；
  - 紫微：表格「项目｜结果」列命宫/身宫/五行局，再两三句解读；
  - 今日黄历：表格列日期干支/冲煞/建除，再各给 2 条宜、2 条忌（不许写空话）；
  - 每日一签解读：就一个吉签级别，不起表格，直接两三句解读+打气。
  这样每次问同一类问题，格式都是一致的，不会有时候给表格有时候纯文字。
- **表格加了点专业感**：塔罗表格里的"正位"/"逆位"字样现在会高亮成不同颜色（正位暖金、逆位暗红），一眼扫过去就知道这张牌是不是反的，不用逐字读。表格本身的样式（圆角、斑马纹、表头底色）上一轮已经做过，这轮延续。
- **顺手又修了一个测试桩的 bug（这次是个大的）**：`textContent` 和 `innerHTML` 之前是两块完全不通的内存——网站里到处用的 `esc()` 安全转义小工具（先设 textContent 再读 innerHTML 拿转义结果）在冒烟测试环境里因此**永远返回空字符串**，意味着之前所有涉及消息渲染的测试其实都是在空字符串上瞎测，测了但没真的测到东西。这次把两者接通（真实浏览器里它们本来就是同一份数据的两个视图），顺手把之前一直没覆盖到的"表格/折叠渲染"补上了正式测试，这块之前一直是盲区。
- **测试**：新增 `test_v466_features.py`（星座 26 个边界日期全核对、结构化模板确实写进了系统提示、脏数据兜底不崩），冒烟新增 `_qRender` 的表格/折叠/正逆位高亮验证，30 → 31 项。

## v4.6.5（塔罗改结合生日 + 发送前先进输入框 + 撤回/重新生成）
- **塔罗不再结合八字黄历，改成结合生日**：之前塔罗和每日一签共用了"结合黄历宜忌+八字流日"这句话，但"结合八字"这个要求原意是给中式玄学的每日一签用的——塔罗走的是西方占卜传统，混进中式八字术语文不对题。每日一签保持不变（本来就该结合八字流日和黄历），塔罗那句改成"请结合我的生日，把这组牌串起来帮我解读"，生日信息本来就已经作为记忆注入到对话上下文里，不用额外接线。
- **抽完不再直接自动发送，改成填进输入框**：塔罗解读和每日一签解签，之前都是抽完立刻调 API 拿回答，你没机会在发出去之前调整今天的具体心情、想问的事情。现在两处都改成把拼好的话填进输入框（用的是已有的 `quillFill`，此前只给"推荐给我""找相似"这类快捷按钮用），你可以接着补一句"最近在纠结要不要跳槽"之类的具体问题，自己点发送才真的发出去。
- **撤回改问 + 不满意重新生成**：聊天气泡现在鼠标悬浮会露出小图标——**你自己发的话**旁边有个"↺ 撤回到输入框重新编辑"：点一下，这条话连同 Quill 对它的回复一起从记录里撤掉，原文退回输入框，改完重新发；**Quill 的回复**旁边除了原来的收藏星星，多了个"↻ 重新生成"：点一下用同一句提问再问一遍，换一个回答。底层是新加的"从某条消息开始，把它和它之后的都清掉"这个接口，撤回和重新生成公用同一套逻辑，服务端记录和界面保持一致，不是只在页面上"看着没了"。
- **顺手修了两个测试桩自己的 bug**：① `createElement` 建的元素之前没有父子关系记录，`nextElementSibling`/`previousElementSibling`/`remove()` 全是假的（remove 甚至是空函数，调了跟没调一样）——加上真实的父子关系登记后这些才是真的在动数据，不是摆设；② `className` 和 `classList` 之前是两份互不相通的数据，用 `.className='qm user'` 赋类名之后 `classList.contains('user')` 永远查不到——这两个坑不止这次撤回功能会踩，往后凡是这么写的代码在冒烟里都测不准，这次一并治了。
- **测试**：新增 `test_v465_features.py`（消息截断的增删边界情况，含跨会话隔离验证），冒烟新增塔罗发送内容+撤回/重新生成的 DOM 联动验证，29 → 30 项。

## v4.6.4（凯尔特十字摆成传统十字+纵列样式 + 面板加大免滚动 + 塔罗背景乐）
- **凯尔特十字改成传统摆法**：之前是简单的 7+3 两行网格，这轮照参考图改成真正的十字+纵列——中间一个十字（目标在上、根基在下、未来—现状—过去横排、挑战横压在现状牌上转 90°，传统摆法里"阻碍压在现状上"本身就是解读的一部分），右侧一条纵列四张（结局在最上，往下是期盼、环境、自身）。阵位名称本来就和参考图是同一套标准位置的不同译法（现状=现状、挑战=阻碍、目标=王冠、根基=基础、期盼=希望or恐惧、结局=结果……），没改阵位含义，只是把摆放方式改对。
- **面板加大，不再硬要滚动条**：塔罗面板从 560px 加宽到 720px（96vw 封顶），配合上面的紧凑摆法，正常桌面窗口高度下十张牌全部能露出来不用滚。极小的窗口（比如浏览器窗口没拉多高）可能还是会露一点滚动条——这是数值算过的，不是没做，只是屋子太小的兜底，不影响正常使用。
- **塔罗抽牌加了背景乐**：打开塔罗面板起渐入（2.4 秒慢慢浮出来，不是一开门就炸响），循环播放，音量压得比较低（0.16，够神秘感不抢戏），关面板渐出（0.8 秒淡出再停）。素材是你直接给的文件，来源标注按老规矩记进了 `static/_sfx/CREDITS.md`（文件名前缀 `freesound_community-` 和之前验证过来源的翻页音是同一套 Pixabay 站内下载命名规律，判断同一来源，免费商用、无需署名）。
- **顺手修了个测试桩自己的 bug**：冒烟测试的假 DOM 环境里，`createElement()` 建出来的元素只有后来手动 `getElementById` 查过的 id 才会被登记，正常代码"建元素→赋 id→塞进父节点"这个顺序建出来的元素反而查不到自己——这次让赋值 `.id` 时就顺手登记，往后这类"先建后赋 id"的元素在冒烟里也能被正确找到、正确验证。
- **测试**：冒烟新增背景乐函数就位+无浏览器音频环境时安静不崩的验证，加上十字组/纵列组坑位数分别核对，28 → 29 项。

## v4.6.3（小修：悬浮气泡有连读天数时看不出能点了）
- **"点进去的彩蛋动画删掉啦？"——没删，是气泡不再暗示自己能点**：`showTermCard()` 这个动画函数本轮完全没动过，字符逐个飞入、节气图、诗句、印章都还在。真问题出在 v4.6.2 把气泡文字精简过头了——有连续阅读天数时气泡里只剩"连续阅读 N 天"，没有连读记录才会带"点开看今日节气"的提示；结果一旦有连读天数，气泡看着就是个纯状态标签，没有任何"点它有反应"的线索，看着像点进去的功能被拿掉了。现在不管有没有连读记录，气泡里都固定带上"点开看今日节气"这句提示，只是仍然不重复节气名和诗句本身（这部分按上一轮的反馈继续去掉）。

## v4.6.2（塔罗解读链路验证 + 每日一签按八字算 + PDF 能拖进库了）
- **"塔罗结果真的能发给 Quill 解读吗"——验证过了，能**：加了一条端到端冒烟，专门盯 `ttSend()` 这条链路：关掉塔罗窗口→打开聊天面板→把这组牌（含逆位标记）连同"结合黄历/八字"的要求一起发出去，三步缺一都会被拦下来报错。之前只是人工看代码觉得没问题，这次是真的跑一遍验证过。
- **每日一签不再是纯抽奖，有生辰记忆时按你的八字流日真算**：新写了 `daily_fortune_level`——用已有的 `paipan_yunshi`（流日对日主的十神、今日冲煞、建除十二神）算一个分数，映射到大吉/中吉/小吉/吉/末吉；同一天算出来的结果是固定的（不是每次刷新都不一样），换个人生辰算出来的级别也会不同。没告诉过我生辰就还是老的加权随机，不会因为这个功能而抽不出签。抽完那句自动发给 Quill 的话也改了，明确要求"结合我的八字流日和今天的黄历，帮我解读一下今天宜做什么、不宜做什么"。
- **"今天都七月八号了怎么还是小暑"——这个不是 bug**：查了实际代码，节气本来就是一段区间（小暑从 7 月 7 日一直到 7 月 22-23 日大暑交接前都算小暑），不是"抽到当天就没了"的一次性状态；卡片上的"7月7日"是这个节气**开始**的日期，不是"今天"，是正常的参考信息。真正的"今天恰好是交节当天"另有一个独立的 `is_term_day` 标记，只在 7 月 7 日当天才为真，用来控制那个额外的轻脉动效果——8 号之后脉动会停，但节气本身还是小暑，直到下一个节气到来。
- **悬浮气泡精简掉了重复内容**：之前鼠标停在浮钮上，气泡里连读天数、节气名、诗句一股脑全印出来，可点进去的节气卡本来就把这些内容做成了动画好好展示一遍——气泡现在只报一句"连续阅读 N 天"（没有连读记录就是"点一下看今日节气"），图标还在（视觉+点击入口），文字不再重复。
- **端口被占用会自动换端口——这个也确认过了，本来就是好的**：`main()` 启动时先尝试收掉自己上一个残留的旧实例，如果端口还是被别的程序占着，会往后扫最多 20 个端口找一个空的，控制台会打印"端口 X 被占用，自动改用 Y"，浏览器打开的地址、uvicorn 实际绑定的端口都用的是换过之后的那个，没有走岔。
- **PDF 终于能拖进库里了**：文件夹扫描入库那条路径其实早就认 `.pdf`（`NOVEL_EXTS` 里一直都有），但**拖拽导入**那个单独的接口一直把 `.pdf` 排除在允许列表外，两条路径不一致——这次把 pdf 加回拖拽导入的白名单。光能"存进去"不够，还接上了阅读器：PDF 用浏览器自带的查看器渲染（内嵌原始文件的 iframe），不用装额外组件、离线也能看；老实说这条路子换肤/划线/自动记进度这些接不上（PDF 就是浏览器自己的查看器在管），跟 epub 目前的已知局限是一个性质，不是装作没有这回事。想要更完整的 PDF 阅读体验（比如自定义主题、划线笔记）得接一个专门的 PDF 渲染库，工作量不小，先不做，这轮先把"能读进去"这一步打通。
- **测试**：新增 `test_v462_features.py`（5 条，含 1 条需要真实排盘计算、其余读源码结构，全部不依赖 fastapi 可直接跑）；冒烟新增塔罗发送链路的端到端验证 + 每日一签的接口消费/兜底验证，26 → 28 项。

## v4.6.1（紧急修复：浮钮点击死锁 + 数据库迁移从没跑过 + 阅读设置不保存）
- **浮钮点击彻底打不开面板（严重回归，v4.6.0 引入）**：上一轮为了让节气插画在悬浮时显示，让点击行为在"悬浮换装中"时改去开节气卡——但 `mouseenter` 必然先于 `click`触发，鼠标点按钮之前必然先"进入"它，等于点击**永远**被拦成打开节气卡，聊天面板从此鼠标点不开了（截图里"只能点出这个彩蛋动画没有对话框"就是这个）。修复：浮钮点击永远只做一件事——开面板；节气卡改由悬浮时弹出的小气泡自己接管点击，气泡和浮钮之间做了个短暂的"鼠标路过缓冲"，从浮钮挪去点气泡的路上不会被提前收走。冒烟里加了一条源码级回归钉子，以后再犯这类"用悬浮状态改点击行为"的错会直接拦截。
- **数据库迁移这一步，真实启动路径其实从没跑过（这是根源性的大问题）**：`migrate_db.py` 负责给库补新表新列（包括聊天要用的 `quill_sessions`），但它只被 `scripts/launcher.py`（一条没被实际使用的辅助路径）和测试脚手架调用过——**`main.py` 真正的启动入口从头到尾没调用过它**。后果：`quill_sessions`/`quill_messages`/`quill_memory`/`reading_prefs` 这几张全靠迁移脚本补的表，在用真实入口跑起来的库里可能从来没被建出来过，聊天直接报 `OperationalError: no such table: quill_sessions`（截图里同学遇到的原样错误），阅读设置也因为 `reading_prefs` 表不存在，每次都读回空的，看着就像"保存了但下次进来又是默认"。
  修了两处、按"治根 + 兜底"两条线一起补：① **治根**——迁移这一步挪到 `init_library()`（库被真正打开的地方，不管是正常启动还是设置页热切换库路径）里，保证只要库被打开过一次，迁移必然跑一次；② **兜底**——`quill_sessions`/`quill_messages`/`quill_memory` 三张表现在跟 `quill_todos`/`quill_habits`（这两张一直是这么做的）一样，在**每次开连接时**顺手自愈建表，不管迁移到底跑没跑过，聊天这条路径都不会再因为缺表而报错。③ **顺手把迁移脚本本身的一个隐患也补了**：之前整个迁移函数只在最后统一 commit 一次，前面随便哪一段中途炸了（哪怕是无关紧要的可选数据，比如给某些老库摸不着头脑的一个历史字段），都会把已经建好的表连带回滚掉——现在关键的几张表建完立刻单独 commit，脆弱的那一段（`mrpro_favorite` 相关）单独兜底不再连累别人。三处都用真实场景复现 + 修复前后对拍验证过（模拟"迁移从没跑过"+模拟"某一段中途异常"两种场景，聊天/表结构都在预期状态）。
- **阅读设置的按钮高亮之前没跟着存档同步**：纸纹/字体/行距这三组按钮，选中后你能立刻看到效果，但重新进入阅读器时——即便这次真的读到了正确的存档值（上一条根源修完后）——按钮本身还是显示着 HTML 里写死的默认选项（比如纹理显示"无"），容易让人误以为"又没保存上"，甚至因此手动点回默认，把好端端的设置点没了。之前只有肤色（皮肤色卡）会在读档后正确同步高亮，这次把纸纹/字体/行距也一起接上；顺手让 `savePref` 存失败时不再默默吞掉——至少打个 console 警告 + toast 提示，方便下次遇到问题时能一眼看出是不是网络问题。
- **翻页音效**：文件本身没被删掉（`static/_sfx/pageturn.wav`，14KB、0.16 秒、真实可播放的 WAV，比 `PAGE_SFX_MAX` 那道 0.5 秒上限还短，符合"就要翻那么一下，不拖泥带水"的要求），触发代码也确认接在真实翻页动作上，没有被误删或漏接。诚实说：静态代码检查没能百分百复现"没声音"这个现象，唯一找到并补上的一个合理疑点是——浏览器要求"用户先有一次真实交互"才允许出声，之前只在鼠标/触摸的第一下交互时唤醒音频，如果有人打开阅读器**第一个动作就是按方向键翻页**（不是先点一下别的地方），音频可能一直没被唤醒过；这次把键盘也加进"第一次交互"的判定里。如果这轮之后同学那边还是没声音，麻烦告诉我是完全没声音还是只有第一次没声音，缩小范围我再接着查。
- **测试**：新增 `test_v461_hotfix.py`（5 条，全部不需要 fastapi、可直接跑）：阅读设置高亮同步的按钮结构核对、`init_library` 确实调用了迁移、聊天三张表确实自愈、迁移函数确实分段提交、`savePref` 确实不再吞错误。加上 v4.6.0 的 33 项，pytest 累计 38 项；冒烟不变仍是 26 项（新增一条回归钉子替换了原有断言，条目数没变）。

## v4.6.0（悬浮换装 + 塔罗洗牌与魔法阵 + 待办日历 + 情绪场感知 + 管理并发）
- **节气插画终于用上了（真问题）**：24 张插画此前只挂在左下角 20px 的小圆点上，等于白画了。改掉——浮钮平时是羽毛，**鼠标一靠近就晃一晃、换成当天的节气或节日插画**（放大到 28px），顺带报连续阅读天数；这段时间点一下浮钮打开的是节气卡而不是聊天面板，离开鼠标自动换回羽毛。原来那个"随机小概率彩蛋"式的悬浮火苗（连读 45% 概率、隔 75 秒）已合并进这个新机制，不再是两套并存的東西。
- **塔罗三件套**：① **洗牌动画**——开牌阵/换阵/再抽一次时，22 张牌先聚成略乱的一沓、整叠"手腕一抖"晃两下，再逐张错开延迟摊回扇形，不再是瞬间贴好；② **牌阵坑位先摆出来**——选定阵型后，每个位置的空槽（现状/挑战/目标…）立刻用虚线卡框铺在一个原创的"魔法阵"背景纹样上，抽到的牌**直接落进它该在的坑**，不用等抽完才知道摆哪；③ 底部加了一圈自绘的椭圆+五角星淡金线描背景，呼应你截图里那种"仪式感摆牌区"的意思（原创画法，没有照抄任何参考截图的具体图案）。随机性本来就有（Fisher–Yates 每轮重洗 + 35% 逆位，v4.5.1 就修了），这轮是把"看得见的仪式感"补上。每日一签的签筒摇动动画本来就有（v4.2 起），这次没再改。
- **掷骰子换成真骰子脸**：⚀⚁⚂⚃⚄⚅ 真点数字符替换掉纯数字文本，滚动阶段做了个 3D 感的翻滚（rotateX/rotateY），停下来一个小小的落地回弹，不再是简单的数字循环+上下弹跳。
- **小游戏结果不再"赢了就忘"**：猜拳/掷骰子分出胜负后，会悄悄写一笔进 Quill 的记忆（不调用 AI、不花 token），下次聊天时它上下文里真的有这件事——问它"刚才谁赢了"答得上来。这条记忆不会出现在设置里的"记忆"编辑列表（内部键，`_` 前缀过滤掉了），不占用户能看到的那份。
- **思考态弹动跟着心情走**：面板头顶那颗灯"思考中"的扩大缩小弹动，此前永远一个节奏；现在借上一次的心情定调子——刚才赢了棋接下来思考会**快而暖**（像小狗兴奋转圈那种急促感），刚被摸头思考会**慢而粉**（带点扭捏），疑惑态思考偏紫、居中节奏，平时没心情记录就还是原来的绿色呼吸。
- **灯光呼吸感补上**：浮钮本体这次也做了一层很淡的外发光起伏（4.6 秒一次，不影响戳它的其他动效）；面板头顶情绪灯的光晕本来只有形状在缩放，这轮让**发光强度本身也跟着一起一伏**（drop-shadow 模糊半径跟着呼吸），两处"灯光区域"现在都是真的在呼吸，不只是珠子在变形。
- **待办 + 习惯统一日历（新功能）**：设置里"待办"标题旁加了个"📅 日历"入口。月视图/周视图切换，每天格子上有待办数量小圆点和"x/y"习惯打卡进度；点哪天就展开哪天的清单——待办可以勾完成/改文字/删，习惯当天可以直接打卡；日历顶部"＋待办"能不经过 AI、不花 token 直接建一条当天到期的待办。到点提醒沿用原有机制（待办到期前 30 分钟浏览器通知、习惯每日提醒），日历只是新增的"看清楚+能编辑"入口，没有另起一套提醒系统。**已知小局限**：周视图跨月份边界那几天只显示已加载月份内的部分（比如这周横跨 7/31 和 8/1，8 月那天会看着像没数据），月视图不受影响；这个边界情况不算常见，先如实交代，下轮想治可以再说。
- **Quill 情绪场感知（本地关键词启发式，不是训练过的模型）**：聊到"难过/压力很大/吵架了"这类偏沉重的内容后 12 分钟内，摸头/猜拳赢这类**外部触发**的开心、害羞反应会先降级成一种安静的"在意你"呼吸（不蹦跳、不闪色），而不是立刻演开心——过了冷却窗口自动恢复正常。这是纯关键词命中做的，做不到真正理解语境，会有漏判和误判，是个诚实的 v1，不是心理健康工具，别指望它准。**配套的"惦记着的事"**：新增 `remember_concern` / `resolve_concern` 两个工具，Quill 聊天时如果注意到你提到一件没聊完、有点分量的烦心事，可以记一笔；**超过 12 小时没跟进**（避免同一次对话里就追问刚说过的话）才会在下次对话的系统提示里出现，提醒它"找机会自然问一句进展，别列清单式逼问"，问完或你说不用管了就清掉，不会同一件事反复问。
  **参考资料**（你问的"有没有论文和项目可参考"）：学术方向叫 *Affective Computing*（情感计算，MIT Rosalind Picard 开创的领域）里的 *Emotion Recognition in Conversations*（ERC）子方向专门做"对话里的情绪随语境变化"，[declare-lab/awesome-emotion-recognition-in-conversations](https://github.com/declare-lab/awesome-emotion-recognition-in-conversations) 这个 GitHub 阅读列表整理得比较全，IEMOCAP/MELD 是这个方向常用的数据集；产品化的参考可以看 Replika——它做了情绪追踪+长期记忆结合的设计（"记得你提过的烦心事，下次主动问一句"和这轮做的 `remember_concern` 是同一个思路），但要提醒一句：Replika 的评价也指出重度依赖 AI 陪伴可能有情感代偿的隐忧，这个项目是个人小工具，量级完全不同，没必要照搬它的复杂度，知道方向就够了。真要做到"理解语境"而不是关键词硬匹配，得让 AI 自己判断情绪（比如每轮回复顺带打一个情绪标签），这样更准但会多花一次调用/更慢，是个可以讨论的下一步。
- **管理任务并发扫描**：「按正文找回/合并作者」这个任务此前是单线程顺序读 13 万+本书的正文开头（纯 I/O 等待），改成线程池并发读（Python 文件 I/O 会释放 GIL，等待时间互相重叠），聚类逻辑本身还是单线程、天然不用加锁。合成场景实测（模拟磁盘延迟）：16 线程比单线程快 12.8 倍，聚类结果逐字节比对完全一致。其余几个管理任务（去重扫描、AI 打标签等）本轮没动，如果也慢可以点名，思路一样能搬过去。
- **节气按农历看？——这个说法本身不准确，顺手澄清一下**：二十四节气其实是**按太阳（阳历）走的**，不是农历。它是把太阳在黄道上的位置每 15° 分一段定出来的，所以每年的公历日期几乎不变（今年小暑就是 7 月 7 日，明年后年也大差不差还是 7 月 6-7 日左右）；农历是"月亮定月份、加闰月凑回太阳年"的另一套历法，节气常年跟着**农历一起印**在传统历书上，容易让人以为它是农历的一部分，但严格说节气的计算依据是太阳，现在的实现（按公历日期匹配）本身是对的，不是 bug。真正**按农历算的传统节日**（春节、中秋）目前还没做（上一轮就提过，需要另外接一套农历换算表），这轮没有主动加，想要的话下一轮点名，我会查权威来源把日期表核对好再接。
- **仍然没动的两块工程债（继续记账，见 v4.5.0 审计）**：`reader.js`（阅读器全部逻辑一个文件）、`mylib_admin.py`（管理页后端没进 routes/ 体系）——这两块拆分适合各占一个专门版本，这轮功能已经够多，没有混进来动它们。
- **测试**：新增 `test_v46_features.py`（日历月聚合走真实接口 / 待办快捷建条与编辑 / 心事记忆的记录与到期清空 / 按正文并发扫描的正确性与加速比），pytest 29 → 33；冒烟修了一个桩环境的真 bug 顺手治好（`innerHTML=''` 之前不会清空模拟 DOM 的 children，导致重复渲染的累积不会被冒烟测出来——这个坑修完对所有用 innerHTML 清空重渲染的模块的测试都更可信了，不只是塔罗）；冒烟 25 → 26。

## v4.5.1（返工轮：六条实测反馈逐一治根）
- **悬浮火苗彩蛋修活（真凶）**：拖动/摸头会置 `window._quillDragged=true`，但只有"随后那次 click"会把它消费掉——触屏拖完、摸完头经常根本不来 click，标志**永久卡在 true**，火苗彩蛋的门从 v4.2 起就一直是关的。现在松手 320ms 后强制归零（click 仍来得及被吞，点击抑制不受影响）。触发参数不变：连读 ≥2 天、至少隔 75 秒、约 45% 概率。
- **节气"换皮肤"看得见了**：此前 11 款佩饰盖 24 个节气（夏至小暑同款麦穗这类），交节当天经常没变化。改成**一节气一款**：新画 19 款 24×24 线稿（雨滴新叶/燕子/纸鸢/牡丹/青麦/青梅/蝉/蜻蜓/西瓜/稻穗/芦苇/桂花/菊花/柿子/山茶/雪人/雪松/水仙/南天竹），沿用 5 款（柳芽/桃花/荷花/枫叶/梅枝），后端映射逐节气唯一；**交节当天佩饰轻轻脉动**（和节日同款待遇）。今天小暑=蜻蜓。
- **塔罗三处治根**：① **之前没有洗牌**——牌堆固定 0..21，不转盘直接抽中间永远是愚者。现在每轮（打开/换牌阵/再抽一次）Fisher–Yates 重洗，一轮内从同一副逐张抽、不放回不补洗（实体塔罗规矩）。② **扇形错乱**——角度做了 ±180 环绕，22 张首尾相接成了环，转远了牌序打结；改成夹限的弧（转到头就停），开局自动把扇转到中段居中。③ **牌被裁一半**——牌区 overflow:hidden 且轴心偏低，中央牌底部和往下拖出的牌都被裁；轴心上移、区高加到 240、拖出的牌不再被裁（可见牌纵向 48..237 全部落在区内，数值验证过）。底部那行"横滑转牌·把中间那张往下拖出即抽"的提示删了——v4.3 本来就删了，是重建时我自己加回去的，错误已认领。
- **批量导入归位**：入口从记忆区下面挪进**表情包标题行**（与＋上传并列）；"（贴 URL 列表）"和弹层里的格式说明行都删了——textarea 占位文本本身就示范了三种写法。
- **情绪光点按验收编排重做**：之前只是两点平移+整体旋转，确实简陋。现在三颗珠（主珠/副珠/小卫星）+ 更浓的 goo：开心=蓄力压一下→分裂两点、小卫星垫下方凹出**微笑弧**→顺时针转两圈→合回**果冻弹**；低落=挂着压扁下沉、晃两下、**滴下一小滴**再浮回；惊讶=挤压拉伸反相的果冻弹、副珠慢一拍出液感；害羞=缩起来发抖+粉光快闪；疑惑=下点沉底做问号的点、**主珠靠自转扫 200° 拖出问号的钩**（小卫星延迟 0.14s 描尾迹）、歪头起伏两下合回。光点 22→26px。
- **小灯颜色回归三态灯**：接好=绿、待命=暗琥珀、出错=红、思考=绿呼吸（此前平时错做成了暖黄，AI 配置好了还黄着，看着像没接上）。情绪色只在表演那两三秒接管，演完 0.5s 流回状态色。
- **冒烟 25 项全部升级重跑**：纹样断言改为"24 节气逐键有款"；塔罗补洗牌是 0..21 排列且五次重开顺序必变、转动夹限、提示行已删；情绪光点补三珠结构断言。

## v4.5.0（遍数新口径 + 白噪音接缝根治 + 线程入库修复 + 工程审计）
- **"读了几遍"重写（口径先定，再谈显示）**：一遍 = 进度**从 0 走到 100**（≥97% 视为读完）。读完那天时间线上打「第 N 遍读完」标；读完之后再翻开，才进入下一遍；没读完的只显示"本遍读到 x%"。旧的"隔 10 天以上算新一遍"是猜法——6/19 翻了 1 分钟、7/05 再读就被算成"第 2 遍"，不对，废弃。
- **顺手修掉 4300%**：同一条进度数据在三处口径打架——阅读器存 0–100 整数，`routes/reader.py` 按 0–1 比较，书架侧又 ×100。现在后端统一归一成 0–100（老数据里 0.43 这类小数自动 ×100，脏数据夹回 [0,100]），前端不再自行换算。
- **网页「开始入库」修复**：`scan_and_import` 里注册 SIGINT 优雅中断（Ctrl+C 跑完本批再退），但管理页的入库跑在服务的**工作线程**里——signal 只许主线程注册，直接 `ValueError` 把整个任务炸掉。现在非主线程跳过注册与恢复（Ctrl+C 本来就只属于命令行），并有测试钉死"线程内入库不炸"。
- **白噪音循环接缝根治**：素材首尾波形对不上，loop 回卷"咔哒"一下。解码后把**结尾约 1 秒与开头做等功率交叉淡化**（sin/cos 增益，响度不塌）再循环——循环点藏进淡化段里，接缝两侧本来就是同一段混合，听不出回卷。只对 >3 秒素材做。顺带把**起播淡入放缓到约 2.4 秒**（缓缓浮上来）、**停止淡出约 1 秒**。「空间」滑块 = 卷积混响的干湿比，属解释性说明，无需改码。
- **测试**：pytest 27 → 29（遍数新口径走真实接口注入日记验证 + 线程内入库），冒烟维持 25。
- **工程审计（只记账，本轮不动刀）**：`reader.js` 2504 行、`mylib_admin.py` 1582 行、`home/index.html` 1829 行（另 `10_shelf.js` 1345 行、`30_theme.js` 1323 行也已越线），均超 800 行红线。建议：reader 按"翻页 / 音效 / 划线 / 设置"拆模块（照 home.js 的拼接路子）、admin 并入 routes 体系——各占一个专门版本做，不跟功能混在一轮里。
- **诚实交代**：本轮新增的 2 条 pytest 在打包环境里没有 fastapi 无法执行，已用桩注入方式对**同一份线上代码**逐场景打靶（7 场景全绿）+ 3 条无依赖测试直跑通过；你本地 `pytest` 或 CI 跑一遍即可最终确认。

## v4.4.0（24 张节气专属图 + 开发方法论 skill）
- **节气卡：11 套通用纹样 → 24 张专属图**：每个节气一张自己的画——立春柳芽 / 雨水雨滴新叶 / 惊蛰桃花 / 春分燕子 / 清明纸鸢 / 谷雨牡丹 / 立夏荷花 / 小满青麦 / 芒种青梅 / 夏至蝉 / 小暑蜻蜓小荷（"小荷才露尖尖角"）/ 大暑西瓜 / 立秋枫叶 / 处暑稻穗 / 白露芦苇 / 秋分桂花 / 寒露菊花 / 霜降柿子 / 立冬山茶 / 小雪雪人 / 大雪雪松 / 冬至水仙 / 小寒梅花 / 大寒南天竹覆雪。风格对齐验收过的荷花 / 枫叶海报版：**半透明色块叠色、柔和渐变、无硬线**；水仙这类白色主体用三层办法保对比（瓣缘一圈很轻的暖灰描边 + 花后淡绿衬底 + 花下深绿叶丛）。轻动效全部复用页面里现成的 qt* 关键帧；渐变 id 带 g01~g24 前缀，同页并存不串色。新模块 `62_term_art.js`（纯图库数据，不塞进 60 撑破行数红线）；`showTermCard` 优先用专属图，旧 11 套留作兜底。
- **开发方法论 skill**：把这一路踩过的坑和长出来的规矩沉淀成 `solo-app-dev-method.skill`——SKILL.md（先对账再动手 / 锚点式修改 / 交付前自测 / 版本纪律 / 打包报告格式 / 沟通偏好）+ pitfalls.md（按症状索引：改了没生效 → 先想内存缓存和僵尸端口；bat 乱码 / pythonw 静默死 / CDN 炸弹 / pointercancel / UTC 日期 / signal 非主线程……）+ design.md（视觉与交互规范：Remix 24 网格线稿、节气图渐变风、"按住才动"手感参数）。以后新开项目或换助手，甩这个包进去就行。
- **冒烟**：21 → 25 里占一条（24 张齐全、键名对齐节气、渐变 id 无冲突）。

## v4.3.0（塔罗进面板 + 情绪光点 + 节日彩蛋 + 解签修复）
- **塔罗抽牌（demo 验收版原样进面板）**：快捷区按钮升级为彩色「塔罗抽牌」，打开下半圆牌扇。**交互一字不差**：按住才动（`e.buttons` 实时校验，指针丢失安全复位）、横滑转盘带惯性、**把中间那张往下拖出才算抽**——跟手放大、过阈值亮金边、不够距离弹簧回位；没有"抽牌"按钮，手感即规则。**四种牌阵**：单张今日指引 / 三张过去·现在·未来 / 五张两难抉择（现状 · 选择 A · A 的发展 · 选择 B · B 的发展）/ 凯尔特十字十张（现状 / 挑战 / 目标 / 根基 / 过去 / 未来 / 自身 / 环境 / 期盼 / 结局，紧凑网格摆开）。抽走的牌从扇里消失、剩下的自动回正补位；35% 逆位。「发给 Quill 解读」把**牌位 + 牌名 + 正逆 + 牌意**拼好，请它**结合今天的黄历宜忌和你的八字流日**一起讲。22 张大阿卡纳图标与扇形数学从验收 demo 原样移植（`65_tarot.js`，全套 tt- 前缀避开已占用的 .stage）。
- **每日一签解签修复（真凶）**：`quillSend()` 从不接收参数，而抽签结果是作为参数传进来的——所以"帮我解读这支签"**从来没发出去过**，面板只是开了个空欢喜。现在带参优先用参数（且不动输入框里你正打的字）；顺手修掉同函数里图片先清后读、**永远发不出图**的老 bug。
- **情绪光点（面板头部那颗小灯重做）**：goo 滤镜流体光点——分裂、合并时会拉出液桥。**六色对六态**：平时暖黄 / 开心暖橙（分成两点转两圈再合体）/ 低落蓝（下沉，左右晃两下，新增）/ 惊讶亮金（光源弹性缩放）/ 害羞粉（快闪）/ 疑惑淡紫（两点摆出问号形，歪头起伏）。**情绪只来自真实互动**：猜拳骰子赢=开心、输=低落、平局=惊讶、摸头授牌=害羞；流程性动作不牵动它。彩蛋：点点摸摸它会开心，3 秒内连点四五下它歪头冒问号。`70_mood.js` 按"后声明覆盖先声明"的拼接约定接管 `qMood`，猜拳 / 摸头老调用点一行不用改。
- **节日彩蛋**：节日引擎补上**万圣节（10/31）与跨年夜（12/31）**；圣诞树 / 南瓜灯 / 跨年烟花 / 情人节心形四款佩饰，节日当天顶掉入口的节气纹样、轻轻脉动，点开还是节气卡。
- **测试**：pytest 25 → 27（节日引擎补日 + 模块拼接含 62/65/70 且按文件名有序），冒烟 21 → 24（塔罗牌堆牌阵状态机 / 情绪光点六态 / 节日佩饰映射各一条）。

## v4.2.0（工程重构 + 节气卡 + 白噪音引擎 v2 + 一批体验修整）
- **工程重构（前端模块化）**：home.js 单文件 30 万字符的时代结束——拆成 `frontend/home/js/` 下 6 个按职责分工的模块（书架 / Quill / 主题 / 历程杂项 / 分享 / 陪伴细节），页面请求 `/static/home.js` 时后端按文件名顺序拼接下发。**不引入任何构建步**：解压即用、离线可跑、改完刷新即生效的哲学不变；冒烟测试同步改为拼接加载。README 新增「🧱 工程结构」一节讲清模块分工与取舍。
- **测试补厚**：pytest 20 → 25 项（习惯打卡全链路 / 热力聚合 / 节气字段 / 连读 / 贴图导入失败路径 / 模块拼接下发），冒烟 18 → 21 项（v4 函数就位 / 11 套节气纹样与 24 条候语 / 热力图 HTML / 本地日期）。
- **节气卡（海报式）**：参考节气海报重画——奶白卡片、大字竖排节气名 + 红印、候语（东风解冻 草木萌动 / 一阴始生 日长之至……24 条）、节气日期；上方是**会动的大纹样**（雨滴连绵落下、麦穗交错摇曳、雪花缓转飘雪、梅枝花瓣飘落……11 套）。**每日播报送达时自动展示一次**（当天只一次，可随播报关），平时**点入口的节气小徽章**随时打开；节气交节当天加注"今日交节"。
- **连读火苗改交互**：右上角常驻角标取消（确实奇怪）。改成**悬浮小彩蛋**：鼠标停到 Quill 上，偶尔（约一半概率、至少隔 75 秒）它会先小小摇一下、再丝滑转一整圈，然后火苗气泡弹出来告诉你"连续阅读 N 天"，停 2 秒自己收走。
- **Quill 设置抽屉**：顶上加了标题栏和「收起 ✕」；打开时占满面板**可以滚动了**（此前内容多了会被裁掉、手机上够不到下半截）；删掉"关掉后右下角浮钮会隐藏…"那行多余提示。
- **读完撒花庆祝可关**：设置里新增开关（默认开）。
- **表情包批量导入修好了**：弹层和三格式识别（描述：URL / URL 描述 / 纯 URL）其实早就在，但后端下载接口一直缺失、且被"抽屉滚不动"挡在视野外——本次补齐 `/api/quill/sticker-import`（逐张下载、只收图片、单张 ≤5MB、重名自动改名、坏链计失败不中断），现在真的能用了。
- **白噪音引擎 v2（手机问题根治）**：不再用 `<audio>` 元素直放——整段解码进 WebAudio（AudioBuffer→BufferSource）。**快慢**变成采样级平滑参数，手机上调慢不再一卡一卡；循环无缝；**音量/远近/空间**全走增益与滤波节点（iOS 上 `<audio>.volume` 本来就是锁死的）；空间感拉高时干声略退，手机小喇叭也能听出区别。个别解不动的编码自动退回老路直放。
- **播放态与展开态解耦**：正在响的音效行常亮一枚**小均衡条**（三根小柱在跳）；细调（快慢/远近/空间）由行尾 **▾** 随时展开/折叠——以前"在播就必须全展开、折叠分组再展开播放态就丢"的两个毛病一起治了。真凶：列表重渲染后负责回写状态的 `syncAmbUI()` 被调用却从未被实现（控制台一直在悄悄报错），本次真正实现：重渲染后把播放态、展开态和四个滑块值全部写回。
- **安卓一键**：新增 `安卓一键部署.sh`——Termux 里一条 `bash 安卓一键部署.sh`：自动装 Python、装依赖、建空书库、拿唤醒锁、起服务；README 方案二同步改写。
- 修复：调高习惯目标后奖牌正确收回（v4.1 已做，本次补了测试钉死）；`/api/quill/season` 补 `date` 与 `is_term_day` 字段。

## v4.1.0（陪伴细节大礼包：习惯打卡 / 节气佩饰 / 连读火苗 / 读完撒花 / 盲盒开卷 / 摸头 / 打盹 / 推送重试）
- **习惯打卡（待办的姐妹篇）**：设置抽屉新增习惯区——建习惯时设名字 / 每日提醒时间 / 目标天数（默认 21 天）。到点系统通知提醒；**月历热力图**（打过卡的日子按次数由浅到深亮起，全打勾当天打 ✓，可翻看往月）；每条习惯带连续天数小火苗、近 14 天点条和**向奖牌推进的进度条**；连续达标首次授予**奖牌**（扫光动画 + 撒花 + Quill 害羞粉闪），中断或调高目标会收回。**晚间小结**（默认 21:30，可改可关）：把"今天还差哪些没打卡"合成一条通知说完，不逐个轰炸。Quill 也长了 habit_add / habit_checkin / habit_list 三个手，聊天里"帮我建个早起打卡"即可。
- **每日播报强化**：不填时间＝默认早上 **08:00**；到点没发成功（接口没额度 / 断网）不再当天作废，**每 15 分钟自动重试**直到发出为止，错过时间点打开电脑也会补发；当天首次失败弹一条说明。修复日期键用 UTC 导致中国时区早八点前可能重复 / 漏推的隐患（改本地日期）。**预置新闻源**：六类 27 个源（中文国际 / English World / 财经 / 科技 / 科学 / 体育生活，BBC 中文、NYT 中文、FT、少数派、Nature、ESPN…）勾选即订阅，自定义 RSS 照旧一行一个；抓不到的源播报自动跳过。
- **读完一本书**：状态点成"已读"瞬间——全屏撒花 + Quill 庆祝抖动 + 弹出**花园里这棵树的特写卡**（用它自己的树种树色，长成完全体），花瓣飘落，副题告诉你这是书房第几本读完的书。（"开花的树"等花园侧做了开花形态后再补。）
- **入口小佩饰**：Quill 浮钮右上角出现**连读小火苗**（连续阅读 ≥2 天亮起，数字=天数，今天没读会从昨天回数）；左下角**二十四节气小徽章**（立春嫩芽 / 春分花 / 谷雨云雨 / 小满莲 / 夏至麦穗 / 大暑蒲扇 / 处暑落叶 / 白露露珠 / 霜降霜晶 / 大雪雪花 / 大寒梅枝共 11 款手调线稿，随节气自动更换，可在设置里关）。
- **摸头**：按住 Quill 不动约半秒＝摸头——粉光一闪、小灯害羞快闪、飘三颗小心心；**摸满 10 次**解锁一句只说给你听的私房话（配彩带）。拖动照旧：按住一动就是拖。
- **打盹**：书架空闲 5 分钟（面板没开、没有任何操作），Quill 垂头轻摇进入打盹——状态灯转暖黄慢呼吸、头顶每几秒飘一个小 z、睡熟了每四十来秒慢慢**翻个身**；任何鼠标 / 键盘 / 触摸立刻惊醒，抖一下毛。
- **盲盒开卷**：快捷区新按钮（手机上**摇一摇**也触发）——礼盒摇晃后随机开出一本**还没翻开过**的书，可"再摇一次"或直接"翻开第一章"进阅读器。
- **修复**：设置页底部版本号卡在 3.83.0 不动（`APP_VERSION` 忘了跟着升级脚本走）；requirements.txt 补上 `requests`（Quill 的 RSS / 天气 / 维基工具需要，源码运行党此前要手装）；tests 里一条早已改名函数的失效断言（flipPage → tryInitStPageFlip）。
- **新增 Dockerfile + README「多设备部署」**：树莓派 / NAS / 旧电脑 / 安卓 Termux 做主机的完整步骤与安全提醒（服务无鉴权，仅限内网或 Tailscale）。

## v3.70 → v4.0.0 要点补账（此前若干轮未及时记档，按对话记录归拢）
- **主题工坊连续大改（v3.70–v3.86）**：草地系统（按月 / 按年一块块草地，可装扮、图片直接拖上去）→ 草地相册（真预览 + 树按住可拖摆造型）→ 主题面板重排为"入口 + 四张迷你地图"→ 编辑器两次脱胎换骨：先照 Photos 改成图标工具栏，再改成**定框拖图**（框常驻居中、拖图挪 / 滚轮缩 / 拖角旋），最后"一张画布"——渐变点 / 雾感 / 质感全部可叠在图片上，渐变点新增浓淡与十三种晕染形状。
- **进程自愈（v3.78）**：心跳看门狗从打包脚本下沉到服务端核心——任何启动方式，浏览器全关 90 秒后进程自动退出、端口释放；端口被占自动后移。
- **Quill 化身重画（v3.88–v3.92）**：树苗 / 表盘 / 月相按 Remix 24 网格 1.5 线宽规范重画；表针坐标 bug、面板 CSS `fill:currentColor` 压盖 SVG 属性等一串显形问题改为内联样式根治。
- **玄学工坊（v3.89–v3.93）**：本地排盘引擎（八字四柱 / 大运流年流月流日流时 / 今日黄历干支宜忌 / 紫微基础盘 / 节气近似），AI 只负责解读；生辰设为最高优先级记忆，记忆可视化管理（改 / 删）。
- **每日一签（v3.94）**：签筒摇动 → 签条弹出（加权随机）→ 全屏彩带 → Quill 粉闪庆祝 → 自动发给 AI 解签；话术区折叠。
- **v4.0.0**：节气 / 节日引擎、RSS 抓取工具、**待办系统**（"提醒我周五交报告"入库、临期通知、面板管理）、每日播报订阅制（天气 / 运势 / 签 / 塔罗 / 英文 / 古诗 / 历史今天 / 新闻 / 自定义抓取）、**猜拳 & 掷骰子**小游戏动画 + 输赢心情灯、多模态贴图仓库、阅读名片 journey/card 后端补齐（6 款皮肤）。

## v3.69.1（学习上限按模型自适应 + 音频空间感/更细调节）
- **学习按钮**改名「学习模式」。
- **读取上限不再写死**：进入学习模式时按当前配置的模型自动取单次可读字数上限（deepseek≈3.2万字、gpt-4o 类更大、8k 小模型更小；未知模型保守取值），前端据此提示。想聚焦某个知识点也可以把范围选得很小——上限只是上界。新增后端 /api/study/limit。
- **音频更细的调节（排版重整）**：每个音效播放时展开的细调改成清爽的带标签三行——快慢 / 远近 / **空间**。
  - 远近：低通滤波，往右削高频，声音更闷更远（像隔着雪或墙，营造"更安静"的下雪感）。
  - 空间：新增混响（Web Audio 卷积 + 干湿混合），往右更空旷、有回响余韵。
- 划线做笔记全链路复查 27/27 通过（选区检测 / 菜单 / 创建渲染 / 笔记 / 拖动手柄 / 删除持久化 / 跳转）。


## v3.69.0（学习模式 + 音频远近/闷滤波 + 知识架构）
- **学习模式（进入式，灵活选范围）**：Quill 面板「进入学习模式」打开浮层，先选 Quill 要读取的范围，再让它 梳理知识点 / 分析知识架构 / 出题自测。三种选范围方式：
  - **按进度**：拖两个滑块选起止，实时预览落在正文哪一段（不依赖分章节，任何书都能用）。
  - **按章节**：起止章下拉（自动定位当前章）；分不出章节的书会提示改用进度/书签。
  - **按书签**：在两个书签之间（不足 2 个书签会提示）。
  - **上下文上限**：显示所选字数，超过单次可读上限（约 8000 字）会提示"只读取前 8000 字"——因为各家 API 上下文有限，范围选小一点更完整。
- **知识架构**：新增"分析知识架构"，梳理这段的逻辑框架/层次脉络（背景→论点→论据→结论 这类），与"知识点"并列，同样可点引文跳转。
- **音频"远/闷"滤波**：除了快慢，新增低通滤波控制（Web Audio）——往右削高频，声音变闷变远，像隔着雪或墙，营造"周围更安静"的下雪感。放在每个音效播放时展开的细调里（慢/快 + 远/闷），不再一排排挤在一起。
- **划线菜单**：彻底移除梳理/自测（还原为 颜色/笔记/复制）；学习相关全部归入学习模式，读取范围由学习模式内部控制。
- **.gitignore**：补充 `.pytest_cache/`。你本地的 build_log.txt / mylib_log.txt / startup_error.log / 安装与迁移指南.md / config.json / build/ / dist/ / .deps_ok / *.db 都已在忽略列表，上传 GitHub 不会带上，无需手动删除。
- 划线/笔记/跳转复核 12/12 通过；跳转两模式（翻页/滚动）一致。


## v3.68.3（学习入口归位 + 环境音面板重排 + 进程/端口自愈）
- **学习功能从划线菜单移回 Quill 面板**：之前把"梳理/自测"塞进了正文划线菜单，这是不对的——控制 agent 读取范围应是 agent 自己的功能。现还原划线菜单为"颜色/笔记/复制"；在 Quill 面板底部新增学习栏：读取范围（当前章节 / 当前位置附近）+ 梳理 + 自测。范围按章节（buildToc 定位当前章节到下一章）或当前位置附近计算，由面板内部控制。
- **环境音面板重排**：原来每行挤着音量+快慢两个滑块、一排排很乱。现每行只显示 名称 + 音量；快慢细调放进一个**只在该音效播放时才展开**的详情区（收着时高度为 0），干净很多。
- **关掉浏览器后进程不退、端口占用——这条 `python main.py` / `启动.bat` 路补上了**：心跳自动退出 + 端口自愈之前只做在了 exe 的 launcher 里，普通启动这条漏了（前端一直在发 /api/heartbeat 但服务端没这个端点、也没看门狗）。现 `mylib_server_v2.main()` 里补上：① 注册 /api/heartbeat + 看门狗（90s 没心跳=浏览器全关→自动退出进程、释放端口，不必再去任务管理器杀）；② 端口被别的程序占用时自动往后找空端口启动，并提示实际端口。
- 跳转两模式复核：翻页与滚动共用同一滚动容器，跳转用 scrollIntoView + scrollTop（overflow:hidden 的翻页模式下脚本滚动同样有效），两种模式一致。


## v3.68.2（划线/笔记跳转定位——真修复）
- **跳转落不到准确位置（手机 / 电脑都是）——根因找到并修**：正文块用 `content-visibility:auto` 只渲染可视块，未渲染块用占位高度 `contain-intrinsic-size`（原为死值 600px）。但一个 4000 字的块真实高度常两三千 px，占位太矮 → 计算目标"绝对坐标"时严重偏短，且该坐标随滚动/哪些块被渲染而漂移，导致跳转落偏（模拟实测误差约 4000+px）。修法两处：① 跳转改用浏览器原生 `scrollIntoView` 把目标滚进视口（原生正确处理 content-visibility 占位 + 滚动锚定），再做至多两次小幅微调落到精确字符行；② 首屏渲染后实测首块真实高度，写入 `--blk-ih` 作为所有块的占位高度（占位≈真实），并保留 `auto` 关键字让块渲染过一次后记住各自真实高度。模拟验证：占位准确后 20/20 落点误差 ≤2px。
- **.gitignore**：补充排除个人迁移笔记（安装与迁移指南.md）与运行日志（`*_log.txt`），上传 GitHub 时不带这些。


## v3.68.1（命名规范化）
- 学习里"出题"相关措辞正式化：选区菜单按钮"出题"→"自测"，弹窗标题"出题考我"→"自我检测"（仍是就地功能，不是单独模式）。
- 完整功能自检：18 项功能（阅读/笔记、声音/外观、背景/封面、管理、学习整套）代码层面逐条核对到位；reader.js 加载零 ReferenceError、服务器 120 条路由全注册、后端逻辑 mock 测试通过。
- **文档同步**：README「功能」段补齐学习功能、背景调节、真实纹理、并发等；修正过时的卷曲翻页（StPageFlip）描述（已停用、阅读器统一到滚动容器）；版本段更新到 v3.68.1；本更新日志补齐 v3.64.25 → v3.68.1 全部条目。

## v3.68.0（自我检测 + 错题本 + 纹理换真实素材）
- **自我检测（出题）**：正文里选一段 → 菜单点"自测" → AI 出若干单选题；作答后提交，自动判分、标出对错选项、给解析、可跳回原文出处、显示得分。仅支持 txt，需在设置里配 AI key。
- **错题本**：答错的题自动收进错题本（复用 reading_notes 表、`source='quiz_wrong'`，未新建表）。笔记页顶部出现"错题本(N)"入口，可回顾错题、跳回原文、移除；普通笔记里自动过滤掉错题不混淆。
- **纹理换真实素材**：把原来的 SVG 噪点纹理换成 PIL 生成的无缝 PNG（纸纤维/亚麻布纹/磨砂颗粒，浅色、multiply 叠加），经现成的 `/api/theme/asset` 路由服务、无新增路由。

## v3.67.1（清理遗留前端死代码）
- 死代码/可达性审计：从活入口做可达性分析，删除 12 个全项目零引用/不可达的函数（其中 `adjustHl` 是划线改拖动手柄后遗留的孤儿，其余是旧 StPageFlip 翻页系统的遗留），reader.js 从 2159 行精简到 1923 行；删后 `node --check` + 可达性复查 + 确认关键活函数齐全，行为零变化。另有一片深度交织、仍被活代码牵连的 StPageFlip 脚手架保留，留待专门清理。

## v3.67.0（学习·知识点带跳转 + Quill 聊天收藏）
- **知识点梳理（带跳转）**：选一段 → 菜单点"梳理" → AI 抽出结构化知识点（标题/说明/原文引文），每个点锚定到正文字符位置、可"跳转原文"直接落到那一行，可单条或全部存为笔记。新增结构化接口 `/api/study/outline` 与 `study_agent.py`（AI 出 JSON，后端用引文在原文反查定位）。
- **Quill 聊天收藏**：每条 AI 回复可点"收藏"、触摸设备长按也可；聊天头部新增"收藏"入口 → 收藏夹，可回看/回填/移除（复用已有的收藏后端）。

## v3.66.0（白噪音快慢 + 预设背景补齐 + 模式切换清理）
- **白噪音快慢速率**：每条环境音在音量滑块旁新增速率滑块（0.5×–1.5×），慢=更低沉、快=更急促（不锁音高，真正改变声音质感）；混音不变。
- **预设背景"选不了"——补齐**：宣纸/抹茶/水彩三个预设引用的图片文件此前根本不存在（`static/themes/` 为空），点了 404、像是选不了。用 PIL 生成补齐三张（暖纸纹/抹茶绿/柔和水彩，平铺的两张做了无缝处理）。
- **模式切换清理**：翻页↔滚动切换时收掉打开着的划线菜单/拖动手柄，避免残留在错位置。

## v3.65.0（背景裁切位置/缩放 + 音效跳前导静音 + 采用语义化版本）
- **背景裁切位置 + 缩放**：主题弹窗新增"裁切缩放"(100–250%)和"裁切位置(左右/上下)"滑块，自己框想显示的部分；缩放以裁切焦点为中心、羽化跟随焦点，书房/管理/设置三处一致生效。
- **翻页音效跳过前导静音**：播放时自动跳过音频开头的静音（内置音效实测仅 2ms 前导静音、非裁切问题；此改是为自定义音效兜底）。真正的翻页延迟根因是上一版改的解码延迟（改用 Web Audio 预解码播放）。
- **版本规则改为语义化**：新增功能升次版本(3.64→3.65)，修 bug 升补丁号(3.65.0→3.65.1)；`mylib/__init__.py` 的 `__version__` 作为唯一版本源。

## v3.64.26（划线自由拖动手柄 + 管理任务并发）
- **划线范围改自由拖动**：点正文里的划线，起止两端出现可拖的圆手柄，直接拖即可自由调整范围（替换原来的"头扩/头缩/尾缩/尾扩"按钮）；拖动用 `caretRangeFromPoint` 把指针实时映射为字符，只重绘划线碰到的块（长篇不卡）。桌面鼠标、手机触摸都可用。
- **管理任务并发**：根因是各后台任务用裸 `sqlite3.connect`、busy_timeout 默认 0，两个任务同时写第二个瞬间撞"database is locked"。统一改为 WAL + 大 busy_timeout（`_lib_conn`）后，互不冲突的任务可并发跑、偶发争锁自动排队等待而非崩溃。实测两线程各写 500 条并发无报错。

## v3.64.25（笔记精确定位到字符行 + 手机做笔记修复）
- **笔记/划线跳转不准——精确到字符行**：跳转原来只二分到 4000 字/块的块顶，块里十几段、落点常差好几屏。改为两段式：先粗定位把所在块滚进视口触发渲染，再在块内按段落 + 用 Range 落到 pos 那一行的像素位置；跳过去还做一次落点淡黄闪烁，一眼看到停在哪。
- **手机做笔记——修**：手机选字常用原生手柄拖出/调整，document 上未必触发 touchend → 笔记菜单弹不出；菜单浮在选区旁又被系统原生菜单挡住。改为触摸设备用 selectionchange 防抖弹菜单、菜单停靠屏幕底部正中（避开系统菜单）、手指一碰菜单即锁住当前选区范围（防点击清掉选区）。


## v3.64.24（目录高亮在读章节 + 去重逐页确认 + 作者找回增强 + 佚名跨状态查重）
- **目录高亮正在读的章节**：打开目录时，当前阅读位置所在的章节会高亮并标"在读"，还会自动滚到可视中间，长书里一眼就知道读到哪一章。
- **去重可逐页翻看、逐组确认**：以前预览只显示前 40 组、其余看不到。现在预览上限提到 3000 组并加了翻页，可一页页翻、一组组确认；任意一行点一下即可改"保留"，跨页都记得住。
- **作者找回增强（治本）**：以前只认书名里的显式标记（作者/by/@）或库里已有的作者名，"书名_作者"这种没标记、作者又还不在库里的大量佚名书都漏了。现在加了按书名词频推断——同一作者名作为首/尾段反复出现在多本【不同书名】里时，认作疑似作者补进匹配集（"多本不同书共用同一名"这条约束能挡掉番外/完结/合集这类反复出现但非作者的词）。仍走预览，过目后再应用。
- **佚名/空作者跨状态查重（补全）**：查重原本按"同作者"分组，佚名书和有作者的同一本书永远不在一组、查不出重复，这是漏的大头。现加第四层：同一书名下若恰好 1 个真实作者加若干佚名/空作者，就把佚名那些当作丢了作者的同一本合并；全佚名（无真实作者锚）的同名书容易撞名，稳妥不在这里合并；多个真实作者撞名也跳过。配合"不确定就并存"，撞名也不会误删。
- 建议顺序：先跑「完结识别」补章节 → 再跑「从书名找回作者」（现在能找回更多）→ 最后「查重」（作者补全后能查得更全）。

## v3.64.23（去重保留算法纠偏 + 不确定多留版本 + 完结识别显示全库累计）
- **去重老留错版本——纠偏**：保留哪一版以前把"字数"当第一信号，但字数常有过时脏数据（0.0MB 的残档却标着 1.7万字），于是把空档误选成保留、把真正更全的 9章 版清掉。现改为以"文件大小"为主信号（最实在的内容量），再看章节、字数；近乎空的文件直接垫底。截图里"哪个龟儿乱扔娃娃""我喜欢上了一个怂货"那种留错的，已纠正。
- **不确定的重复，多留几个版本**：以前每组只留 1 本、其余全清，但有时几个版本各有所长（一个章节多、一个字数多），机器拿不准谁更全。现改为只清两类——近乎空/碎片，以及在 大小+章节+字数 三项上都不优于保留版的（严格更差）；其余在某一维更优的会标"并存"一并保留，不再误删。预览里能看到"保留 / 并存 / 清理"三种，点任意一行可手动把它设为唯一保留。
- **完结识别看着只有 2 本——其实是显示问题**：完结/连载/未知的计数以前每次运行从 0 算、只统计"本次扫的书"。13 万本若之前已扫完，这次续跑只新扫了 2 本，就显示"完结 2"，看着像全库只有 2 本完结。现在改为同时统计并显示"全库累计：完结 X、连载/未完 Y、未标记 Z"，是真实总数。

## v3.64.22（exe 自动退出 + 手机划线修复 + 阅读进度真实统计）
- **关掉浏览器后进程不退、再开打不开——修**：exe 的 launcher 以前既不查端口占用、也没有退出机制，旧实例一直占着端口，再点 exe 就 bind 失败、像"打不开"，得去任务管理器杀。现加两道：① 启动时若端口已被占用（说明已经在跑），直接打开浏览器连过去、不再重复启动；② 心跳自动退出——网页每 20 秒 ping 一次，看门狗发现 90 秒没人 ping（浏览器全关了）就自己退出进程、释放端口。手机锁屏/切后台时定时器降频，90 秒宽限不会误杀；切回前台立刻补一次心跳。
- **苹果手机划线划不上、还自己往下滑、点不开菜单——修**：选字续滚那段是给桌面鼠标拖拽设计的，在 iOS 上会和 Safari 原生的长按选字 + 选字滚动打架，形成双重滚动，于是选不上、页面乱滑。现加触摸设备判定，手机/平板上一律交给系统原生选字，不再用自定义续滚。另外划完留下的选区以前会把后续点击也拦掉，导致点不开菜单；现改为干净轻点会先清掉残留选区再正常呼出菜单。
- **在读的书进度全是 30%——修**：书房接口 `/api/shelf` 以前根本没读 `reading_progress` 表，前端只能拿"阅读时长"瞎估、估不出就兜底成 30%。现接口按每本最新一条真实进度返回（优先用存的百分比，没有就用 阅读位置÷总字数 现算），在读无记录的显示 0% 而不是假的 30%。

## v3.64.21（翻页彻底重写为最稳做法 + 修打包 Qt 冲突 + 换图标说明）
- **翻页"闪一下不动"——根因 + 重写**：上一版(.20)用"`.page-sec` 分页 + `scrollIntoView`"翻页，分页是按另一个盒子(`.page-sheet` 680px)算的、塞进滚动容器后高度对不上，`scrollIntoView` 经常定位不动，但淡入动画照放 → 就是"闪一下没反应"。本版把这套整个删掉，改为最稳的做法：**翻页与滚动共用同一套连续正文(.rblock)**（划线/定位完全一致）；翻页模式用 CSS `overflow:hidden` **禁掉手动滚动**（满足"翻页时不该能往下滚"），点左右用脚本 `scrollTop` 把容器**整屏换到上/下一页**（`overflow:hidden` 不挡脚本滚动 → 一定换得动），配 0.14s **交叉淡入淡出**遮住跳变 → 读着是"翻了一页"而非"在滚"。每翻一页 = 一屏正文、留一行重叠（不丢行/不切行）；连点有锁防错位。
- **翻页划线跨页续滚**：划线拖到屏幕下沿时，计时器照旧自动续滚（也走脚本 `scrollTop`，`overflow:hidden` 下有效）→ 可跨页连续选到需要位置；松手即停。
- **翻页↔滚动切换零跳变**：两种模式 DOM 完全一样，切换只切"能否手动滚 + 点击是否换页"这个行为，不重渲染、不跳位、正文停在原处。
- **打包报错"multiple Qt bindings"——修**：你这台全局 Python 同时装了 PyQt5 和 PyQt6（科研环境的其它包），PyInstaller 不允许一个程序打两套 Qt → 直接中止。`MyLibrary.spec` 加 `excludes` 排掉本应用根本不用的 Qt/numpy/matplotlib/pygame/IPython/PIL 等大包（已逐个核对无 import）→ 修掉中止，exe 还更小。
- **`build_exe.bat`**：全程写 `build_log.txt`；并修掉"既报 DONE 又报 NOT produced"的括号转义 bug（改用 goto 标签，成功/失败只报一句）。
- **换图标**：exe 图标取自 `static/app.ico`。换图标 = 用自己的 `.ico` 覆盖这个文件后重打即可（没有 .ico 工具的话，给一张 PNG/JPG，可代为转成多尺寸 .ico 放进去）。

## v3.64.20（在 v3.64.19 上：翻页改"瞬间换页" + 加云端打包）
- **翻页"不像翻页"——改对**：上一版翻页是"平滑滚动一整页"，看到的是滚动动作、读着仍像在滚。改为**瞬间翻到上/下一整页**（scrollIntoView 直接定位、无滚动过程，像 Kindle 啪一下换页）+ 一个 0.2s 极短淡入，读着就是"翻页"。点左右/方向键各翻一页；划线拖到页底续滚跨页不变；滚动模式不动。
- **云端打包（你零安装）**：新增 `.github/workflows/build-windows-exe.yml`。推到 GitHub 后，在仓库网页 Actions 里点一下 Run workflow，GitHub 的 Windows 机器会自动打包，几分钟后在 Artifacts 里下载 `MyLibrary.exe`——你本地不装 Python、不敲命令。

## v3.64.19（基于你手上的 v3.64.16，专修三处：划线定位 / 拖书无反应 / 翻页）
- **划线定位差很多——真修**：① 开书时统一换行符（\r\n/\r→\n），让"原文字符偏移"和屏幕渲染严格对齐；② 不再只校正"没有位置的老划线"，改为开书时按每条划线的文字在正文里重新定位（recoverHlOffsets 全量重锚），历史版本里按错误偏移存下的划线（带着错的 start）也会自动归位，不必删了重画；③ 选区取偏移补上 .page-sec 锚点。
- **拖书换作者"完全没反应"——根因（确定）**：搜索状态下界面用 SEARCH_RESULTS 渲染，而 dropToAuthor 只改了 DATA（两份是不同对象），所以拖确实生效、改的那份却没被画出来。新增 updateBook() 让改属性时 DATA 与 SEARCH_RESULTS 两份同时改（dropToAuthor／收藏／打标签均走它）→ 搜索状态下拖完立刻看到移动 + 目标排高亮闪。
- **翻页改回真·一页页翻**：删掉"点右边平滑滚一屏"。翻页模式改为复用按行测高分页 paginate() 把整本切成"整页"（.page-sec，每页=视口可读高、断点在行边界不切行），点左右／方向键＝翻一整页（吸附）。只有划线选区拖到屏幕下沿那一下才自动续滚到下一页、可接着划到合适位置（跨页划线）；松手仍是翻页。**滚动模式完全没动，是安全兜底。** 翻页↔滚动切换、改字号/行距/字体、缩放窗口都会重排并回到当前阅读位置。
- 注：本版基于 v3.64.16，未并入 v3.64.17/.18 的云端打包/exe 相关改动（那是另一条线，待你需要时再弄）。
- 一键 exe（给朋友零门槛用）：① launcher 首次运行**自动新建书库**（建在 exe 同目录，不可写则退到用户主目录）→ 朋友双击即用、不用再选文件夹；② `scripts/build_exe.bat` 重写为**纯 ASCII + CRLF**（修掉中文被 CMD 当命令执行的报错），并**自动探测 Python**，缺失则打开官方下载页。注：生成 Windows 的 .exe 仍需在 Windows 上跑本脚本（或用 GitHub 云端打包）。

## v3.64.16
- 一键 exe（给不装 Python 的朋友双击即用）：修好打包链路 —— 新增正确的 PyInstaller 配置 `MyLibrary.spec`（打包前端/素材，补全 uvicorn 与 routes 包等动态/扁平导入的隐藏依赖）；修 `scripts/launcher.py`（打包态把 MYLIB_FRONTEND/STATIC 指向 _MEIPASS；首次运行允许选空文件夹**自动新建空书库**，全新用户也能用）；重写 `scripts/build_exe.bat` 为一条龙；新增 `scripts/打包成exe指南.md` 傻瓜教程。已在本机实测打包链路：产物能起服务、正确发出前端首页（HTTP 200）。注：Windows 的 exe 需在 Windows 上用本配置打包。

## v3.64.15
- 阅读器修复：① Quill 小窗关闭 × 误调 toggleDrawer（其实是“打开”）→ 关了又重开，改为 closeAll() 真正关闭；② 划线“差一行/跳转不准”——选区偏移原按整块(.rblock)渲染文本长度算，而渲染跳过空行致偏移偏小；改为按所在段落(.rp，data-off 即原文起点)分别算 start/end，空行不再使偏移错位；③ 点笔记/划线跳转统一按字符偏移滚到位。
- 翻页改造（按需求割舍卷书翻页 + 要能跨页连续划线）：**彻底停用 StPageFlip 卷曲翻页库（含移除其 CDN/本地脚本与轮询），把“翻页模式”重写到与滚动模式同一条滚动容器上 —— 点左/右 1/3 = 平滑滚动一屏（带翻页音）、中间 = 呼出工具栏；底层是滚动 → 划线拖到屏幕边缘会自动续滚，可在翻页模式里跨页连续划线、划到合适位置为止。** 好处：翻页瞬间、不再等 CDN、断网也不空白；翻页↔滚动切换不重渲染、不跳位、瞬间完成；改字号/行距/字体与缩放窗口后自动回到当前阅读位置。
- 书房拖书换作者：之前改完数据虽重画，但目标作者书架按完结/收藏排序、刚移入的书排到横向滚动视野外、又无滚动/高亮 → 看着像“没反应”。现加“移动中…”提示、等服务器写回完成再提示、并**自动滚到目标作者那一排 + 整排高亮闪一下**（书仍按原排序、不强行提到最前）。

## v3.64.13
- 素材：放入 static/_ambient/ 8 个 Pixabay 白噪音——雨声/细雨（夏雨 4:07、Gentle Rain 1:30）、海浪/浪花（3:00、0:22）、林间风（2:27）、森林鸟鸣（1:14）、壁炉（4:23）、咖啡馆（0:20）；均为 Pixabay Content License（免署名、允许随作品分发），已在 static/_ambient/CREDITS.md 逐个登记原始标题/上传者/链接。显示名用中文，后端按目录动态加载（无需固定文件名，加新白噪音直接丢进该目录即可）。
- 翻页音更正与重裁：static/_sfx/pageturn.wav 实为 **Pixabay "Paper turn"（freesound_community）下载裁剪**而来（此前 CREDITS 与 README 误记为"代码自合成、MIT"，已更正为 Pixabay Content License 来源，并补上原始链接 https://pixabay.com/sound-effects/household-paper-turn-40077/ ）。从该 10.7s 原文件里挑出最干净的一下翻页重新裁出约 0.16s、掐到清脆声起音处开始并加淡入淡出，翻页时声音即时响起、消除此前的延迟/空白感。
- 仓库精简（为上传 GitHub）：移除 docs/archive/（85 份逐批开发笔记，更新日志已汇总）、安装与迁移指南.md（针对旧机器的个人迁移笔记）、docs/总体规划与进度.md（已过时、与 README 重复）、docs/GitHub上传指南.md。保留 README / LICENSE / 本更新日志 / 两个 CREDITS / vendor 说明 / 设计图。
- 代码逻辑无变化。

## v3.64.12
- 最大化空白加"终极兜底":重建翻页后核验当前页若仍无文字,再补建一次;还空则软刷新一次页面(进度已存,重载后自动回到当前位置;首次建翻页那条路确定能正常显示,故不会再空)。用 sessionStorage 时间戳防止反复重载(8 秒内至多一次)。正常情况下 v3.64.11 的修复已让它不空,这条只在极端残留时才触发。
- 仓库上传准备:新增 LICENSE(MIT);补 .gitignore 忽略 .deps_ok;新增 static/_sfx/CREDITS.md（翻页音=自合成）与 static/_ambient/CREDITS.md（白噪音素材登记模板+可否公开重分发的判断）;README 增加"许可与素材"说明。

## v3.64.11
- 终于定位并修好"小窗最大化后整页空白"(真·根因,在 page-flip 库):库的 setHandlers() 总会 window.addEventListener('resize',onResize),但 destroy() 只有在 useMouseEvents 为真时才调 removeHandlers() 去摘它;我们用 useMouseEvents:false → 那个 resize 监听【永不被移除】、一路累积。最大化时旧实例的 onResize 仍按"放大后的容器 vs 建实例时的固定页尺寸"重算把页面挪出可视区 → 整页空白、页码冻住不变。修法:① destroyFlip 在 destroy 前临时把 useMouseEvents 置真,逼库执行 removeHandlers() 摘掉它注册的 resize 监听(顺带要移除的鼠标监听本就没注册,无害);② 我们自己的 resize 监听(脚本初始化即注册,早于库的 onResize)里先 destroyFlip——本次 resize 里它先执行并摘掉库的监听,库的 onResize 还没轮到就被移除,于是最大化不再挪位空白,随后防抖按新尺寸重建。
- 说明:v3.64.9 修好了前后柔软翻页(已确认);v3.64.10 对字号重排补了重建时序;本版才命中"最大化空白"的真正机制(此前一直以为是重建时序,实际是库泄漏的 resize 监听在作祟)。

## v3.64.10
- 真正修好"放大字号后整页空白"(v3.64.9 只对了一半):根因在 page-flip 库——loadFromHTML 里 pages.show(startPage) 同步执行,但定稿布局/页位的 ui.update() 走 setTimeout(0) 晚一拍;重建时我们的 turnToPage 赶在布局定稿前跑,当前页摆位错/被隐藏成 display:none → 整页空白(页码却仍显示 3/243)。改三处:① 建实例时直接 startPage=当前页,让库自身的首帧就摆对页;② loadFromHTML 后在库的 setTimeout(0) 之后与再下一帧各补一次 turnToPage;③ 字号/字体/行距改动与窗口 resize 的重建统一改为"双 rAF 等布局稳再建 + 建完核验当前页是否真有字,空了再补建一次"(与首次建翻页同款时序),不再同步重建。
- 向前/向后柔软翻页:沿用 v3.64.9 的修复(关掉书角判断),已确认两向都正常。

## v3.64.9
- 修小窗只能往后翻、翻不回去(根因):page-flip 的 flip() 有"落点是否在书角"判断,disableFlipByClick:true 时 flipNext 落点在右下角(过)、flipPrev 落点{x:10}在左缘,竖排单页下被判为不在书角而直接 return→向前翻没反应;因 useMouseEvents 已为 false(库不监听鼠标/触摸),关掉该判断不引入误翻,只让 flipPrev/flipNext 两方向都生效。flipPage 的 return 移出 try,翻页抛错时不再落到隐藏的 page-stack 上"翻了却空白"。
- 修放大字号后整页文字消失:setFontSize/setFont/setLineHeight 由"paginate 后 showPage(在旧分页的卷曲实例上 turnToPage→内容溢出被裁)"改为重排后 initFlip 用新分页重建(字号滑块连发,防抖160ms)。
- 收藏翻阅翻页改柔软:替换 rotateY(±90°)立起对折的硬翻,改为贴左侧书脊轻抬、向外滑出淡出、新页从另一侧柔和滑入归位,与阅读器一致。

## v3.64.8
- 修卷曲翻页两页/放大空白/小窗翻不动:StPageFlip 由 size:'stretch'(按容器自决1或2页、resize不跟手→空白)改为 size:'fixed'+usePortrait 强制固定单页;窗口变化按新尺寸确定性重建。

## v3.64.7
- 翻书声修复:合成一段翻页音效(static/_sfx/pageturn.wav,约0.5s)随包发布,开箱即响;/api/sfx/pageturn 路由改为也服务.wav并允许用户mp3覆盖。
- 柔软卷曲翻页修回:删掉 tryInitStPageFlip 里"检测到landscape就destroyFlip退回"的过敏安全网(usePortrait已强制单页),它会在布局未稳时误杀正常的卷曲翻页——即用户说的"短暂修好又被改坏"。

## v3.64.6
- 修页面底部空一块:paginate不再把availH压到flip盒子矮高(flipAvailH改=boxH-padT-padB),StPageFlip flip-book高度改满高(window.innerHeight),两路同高。
- 修翻页无动画(疑):showPageSimple折页启动改双rAF→强制回流(void offsetWidth),避免webview跳过过渡。
- cycleStatus加saveShelfCache持久化状态。
- 待确认:Windows显示动画开关、移动端翻页/滚动模式、具体哪种拖不刷新。


## v3.64.5
- 卷曲翻页自动回归:加 watchFlipLib 轮询(≤6s),CDN(jsdelivr/unpkg)异步到货后自动 initFlip 升级成StPageFlip卷曲翻页,无需手动放vendor文件;保留单页约束+landscape退回保险。
- 双击作者名修为真·全部作品:gotoAuthorPage 先 switchRoom('library')+view='author'+清tag筛选 再全库搜,不再被当前收藏/书库视图限制。
- 作品详情去掉多余「✎改」,作者名双击=跳转。


## v3.64.4
- 查明软翻页一直没有的根因:StPageFlip只走CDN(unpkg国内连不上)+本地未放+脚本defer晚于READER_INIT。改:内置翻页柔化(perspective2000→1500/缓动更软更长.62s/fold-shade顺背光渐变);页面脚本本地版改非defer且在reader.js前加载(放了vendor文件即生效);加jsdelivr CDN备选。
- 加 .gitignore 加固(config.json/*.db/*.bak/_task_state等不上传);GitHub上传指引。

## v3.64.3
- 作品详情弹窗:作者名双击改为跳转作者全部作品(gotoAuthorPage),原"双击改作者"挪到名字后的小「✎改」;作者名加虚下划线提示。


## v3.64.2
- 双击作者名→作者主页:触发按作者名的全库搜索,把该作者全部作品堆一起(shelf-author span 加 ondblclick=gotoAuthorPage,默认视图+收藏视图都有,加cursor/title提示)。


## v3.64.1
- 收藏翻页改为左侧书脊为轴的翻页(立起edge-on时换内容再落下),去掉甩过头的wobble,阴影加深;swap对齐到edge-on中点。
- 拖书到收藏:加 saveShelfCache+清选区+updateFilterSummary+render,修拖完不刷新/被缓存覆盖。
- 待用户定位:拖动刷新(具体哪种拖)、手机卡(开App/滑动/阅读器哪一步)。


## v3.64.0
- 新增「同名书笔记互通」任务+管理卡:按归一化书名(认精校/全本/番外等版本标注)+同作者聚类,把已有版本的划线/笔记/书签/简评/评分/收藏/已读/进度【复制】给同组空白版本(非破坏,两版都留,执行前备份)。复用dedup的_cluster_by_title;新增_copy_user_data(复制非搬移)。
- 答疑:屏幕自适应已于v3.63.3完成;版本管理(去重隔离区+数据搬移)已存在;管理任务并行未做(单连接写库,不安全)。


## v3.63.3
- 找回卷曲翻页:重新启用StPageFlip(之前停用因缓存旧码误判两页),加两道保险(容器锁单页宽+建好后查朝向,landscape则退回单页翻折),保证不再两列。flip-page改用renderParas+对齐内边距。
- 页宽自适应:按窗口~66%取值(夹700~1040),修"只占半屏";resize重排改为initFlip重建。
- 待办:管理任务并行、拖动后即时刷新。


## v3.63.2
- 按Moon+改分页排版:每个自然段成段+段间留白+首行缩进(沿用原文全角空格),连续空行合并为统一段距;翻页/滚动两模式都用;分页测高同步按段落结构量,不切底部字;划线/调长度/改色照常(字符偏移精确保留,已测)。


## v3.63.1
- 划线调长度:小菜单加 头扩/头缩/尾缩/尾扩(按标点一句句扩缩,可连点,即时重画+存盘);PATCH接口放开position/text。
- 分页边距加宽(28→34)更舒服。


## v3.63.0
- Quill 改为可拖动小窗(右下角圆角小窗,按住标题栏拖动,记住位置,点关闭/历史/新对话不误拖)。
- 待办:划线调长度(拖拽handle)、分页行距边距按Moon+细调(待用户指方向)。


## v3.62.9
- 阅读器进度条：呼出工具栏后移到标题栏下方+变粗+圆点常显,看得见随手拖;细线态也可拖。
- Quill/列表抽屉层级提到进度条之上,修"关闭叉被进度条压住点不到"。
- 点正文里的划线弹就地小菜单(改色/笔记/删除),参考Moon+。


## v3.62.8
- 挖出"改了没效果"根因=桌面webview/SW缓存旧reader.js。破缓存:reader页/reader.js/home页/home.js 全 no-store,且 reader.js/home.js 注入每次启动变的 ?v=;SW缓存版本v6→v7启动清旧缓存。
- 翻页彻底弃用StPageFlip,改自控单页立体翻页:永远单列居中(参考Moon+),点页左1/3上一页/右1/3下一页/中间菜单,切换不再空白。


## v3.62.7
- 修翻页/滚动来回切换后整页空白：initFlip 加"仍在翻页模式才建"守卫+容器未布局则等帧重试；destroyFlip 清掉 StPageFlip 残留类名/内联样式。
- 确认往前翻已接好(flipPage(-1)→flipPrev,键盘←/点书左1/3)；之前没反应是空白bug所致。
- 改阅读状态后任何视图都即时重排(原只书架视图刷),书实时挪分区。


## v3.62.6
- 真正修复阅读器"宽屏摊成两页/往前翻不了"：根因是书容器一直满窗宽(StPageFlip 容器≥单页宽×2 就两页)。改为把容器锁成单页宽居中+minWidth=0.72容器宽→强制单页竖版(1366/1920/2560验算)。点击翻页改按书分区(书左1/3上一页/右1/3下一页/中间菜单)，修"点书左边没反应"。


## v3.62.5
- 列出全部总评：不再悄悄去重，每条标来源(当前库/各备份)、同条多来源全列出、内容不同分开列(与作者无关)。
- 收藏翻页改成柔软卷页动画(以书脊为轴卷走→换内容→卷回,方向感知+翻页阴影)。


## v3.62.4
- 修"快捷方式点不开"：pythonw 无控制台时 sys.stdout=None，uvicorn 调 isatty() 崩溃。除 main.py 已有保护外，又在 mylib_server_v2.py 的 uvicorn.run 前兜底接流，旧 main.py 也不怕。已模拟测试。用户那份报错证实其跑的是旧 main.py(server.main 在73行而非90行)。


## v3.62.3
- 找回简评加「列出全部总评（自己复制）」：把当前库所有副本+所有备份里的总评全文列出(书名+全文去重,不筛选),每条带复制按钮,手动粘贴回去。已单测。


## v3.62.2
- 找回简评新增"库内同名副本找回"：总评常写在重复书的另一副本上，现在能把有总评那份补给同名为空的那本(不依赖备份)。配合每日自动备份，找回更全。已单测。


## v3.62.1
- 查清"找回简评=0"：保存/去重/导入三条写总评的路径都不会丢/覆盖简评(导入只回填非空)；找回扫不到是因为以前没自动备份、或那本本就没写过总评(评分≠总评)。
- 真正的修：加每日 library.db 自动备份(后台、sqlite备份API兼容WAL、留最近5份、bak_auto_*)，今后丢了可从备份找回。


## v3.62.0
- 去掉"已载入全部X本"提示；总评从阅读历程总览挪到收藏每本顶部(改用真实总评接口数据+星级)；收藏支持左右滑动翻页。
- 修翻页盖字/切行：分页严格按真实页宽680算(原来按整窗算→宽屏溢出被切)；强制单页竖版(maxWidth 1000→720)，宽屏书居中两侧留干净点击区，修往前翻不了/横版两页盖字。


## v3.61.1
- 修"打开书房很慢"：桌面端改成先取1200本秒开、其余后台补齐(原来全量阻塞)；/api/shelf 分页时只对本页书聚合笔记数/体裁(分批IN,不再全表GROUP BY/JOIN),首屏大幅加快。已单测。


## v3.61.0
- 找回早期可用的卷曲翻页：恢复 StPageFlip(CDN+本地vendor兜底)、翻页改回 flipNext/flipPrev(早期同款带卷曲动画)、手机也用真卷曲(去掉isNarrow排除)、页数上限240→1500、分页兼顾卷曲盒子高度防切行。库都拿不到才退内置CSS 3D翻页(不再瞬跳)。


## v3.60.0
- 翻页从平移滑入改为内置「CSS 3D 立体翻页」(整页绕书脊翻折露出下一页,离线不依赖外网库)；点击翻+滑动跟手翻都走立体效果。
- 可选真·卷曲：留本地接口/static/page-flip.browser.js+frontend/vendor/，放入 page-flip.browser.js 即自动启用软卷曲,没放则用CSS 3D,绝不报错。
- 跨页做笔记建议用滚动模式(已实现自动续滚)；翻页模式跨页选字待用户确认后专做。


## v3.59.0
- 跨页做笔记改用自然方式：删掉两步按钮，滚动模式下拖选到屏幕边缘自动滚动续选(松手停)；翻页模式因每页独立、选区无法跨翻页，跨页连续划线归到滚动模式。
- 翻页动画修对覆盖式：底层改垫"被翻走的那页"(原来垫错成下一页)，滑动不再闪错内容，带前缘阴影；这是微信读书"覆盖"同款、纯CSS离线稳。


## v3.58.0
- 跨页划线/做笔记：起点页选中点「跨页」→翻到结尾页选中点「划到这里」，跨任意页的整段一起划上，可写笔记/改色；跨页划线在每页正确分段显示。
- 自检 19 项全部在位、编译通过、无孤儿函数。


## v3.57.0
- 翻页动画修复：原翻页库依赖外网 CDN，局域网/离线下载不到→退化成瞬间跳页。改用内置"单页滑入"翻页(离线可用、各设备一致、带前缘阴影)，删掉连不上的外网脚本。
- 切页不再切半行：分页从"按固定字数"改成"量真实高度分页"，按屏幕自动贴合、尽量断在换行处；横竖屏/改窗口/改字号会自动重新分页。


## v3.56.0
- 多选栏加「标已读/标在读」批量改阅读状态；收藏每本最上面显示该书总评+顶部加搜索栏跳转；数据库读加等锁(维护任务跑时搜索不再失败)；完结识别加「停止」按钮(所有任务停止键统一可用)。


## v3.55.0
- 笔记/划线重做：列表默认不摆改色点，点编辑才展开"随输入变高的笔记框+改色"，去掉白色 prompt 弹框，划线点击可跳转。
- 长按不再误翻页（选字/长按/移动都不翻）；翻页跳页加严识别。
- 章节识别支持纯数字分章(1 2 3)，目录和理清章节都认，且不误判零散数字。已单测。
- 找回简评：按真正写入行数计数+列出恢复了哪几本+加锁，避免"报成功却没写入"。


## v3.54.1
- 安装到桌面按钮只在真能安装时显示（独立窗口里不再多余）；进管理加统计缓存(秒显，不每次全库重算)；管理页按流水线从上到下重排；阅读加"正在加载 X%"+超时提示，不再白屏无反应；找回简评加按书名兜底（作者被改名也能找回）。


## v3.54.0
- 新增「找回丢失的简评（从备份）」：扫数据库历次备份(library.db.bak*)，把"备份里写过、现在库里为空"的简评找出来、可写回，不覆盖现有。逻辑已单测。管理页新增卡片。
- 整理流水线收尾：⑦"去重后只给没标过的打标签"确认已具备（打标签默认范围=未打标签）。①~⑦ 全部到位。
- 全功能复查 18 项全过；又清掉 3 个死函数(renderBookmarks/renderHighlights/setAmbVol)。复查确认：翻页前后都有动画且有"保证翻动"兜底；移动端 PWA（manifest+sw+安装入口+分平台指引）齐全。


## v3.53.0
- 整理流水线 ⑥「版本沿用旧笔记」：去重删掉旧版本前，自动把那本上的简评/评分/收藏/已读/最近阅读/标签/划线/书签/笔记/进度搬到"保留"的版本，不再丢。规则：简评保留项空才用旧的、评分取高、收藏已读取或、状态取靠后、时间取更晚、标签并集；位置相关数据(划线/进度等)在保留项为空时整体迁移。逻辑已单测。
- 去重卡片说明更新，标注"数据会保留"。


## v3.52.0
- 删掉书详情里多余的「移出在读」按钮（状态那行点一下就能切 未读/在读/已读，不用重复入口）；连带删掉它的死函数。
- 管理面板「由浅入深」重排（初版）：顶部加「整理流水线·推荐顺序」导航 1→7，点一步跳到对应卡片并高亮；卡片加了锚点。明确「完结识别 + 分章节」是同一步（章节数在这里批量存）。
- 全功能再次复查在位（19 项），无重复函数、无失效引用、无孤儿。


## v3.51.0
- 全功能复查：把之前几轮说过的 26 项改动逐一在代码里核对到位（清单见更新说明）。
- 划线挂不上：再加固——选区找不到带偏移的块时，不再用当前位置乱猜（那会挂错/挂不上），改成在正文里就近定位选中文字；划线没接住会有提示。
- 清理死代码：删掉 9 个没人调用的旧函数（goShelfHome/moonName/confirmRemoveTree/renderJournalBook/groupByWeek/quillAsk/setJourneyView/setTreeSkin/openBooklist）。复查无重复函数、无失效按钮引用。
- 版本号定死用 SemVer（v主.次.修），不再换格式。


## v3.50.0
- 版本号改细：改用 v主.次.修 三段，小改动走最后一位（之前一轮不管大小都占一个版本太粗）。
- 阅读器：进度条可**直接点/拖跳读**（顶部细条，悬停/拖动时出现圆点把手）。
- 阅读记录：**种树**区也按**最新阅读**排前（和书架一致）；**「生成书单」移到这里**（不再挤在多选栏），用读过/在读的书生成。
- 每本书新增**「阅读回顾」**：详情里显示读过几天、估算读了几遍，点开看时间线（第二遍/第三遍从哪天开始）——数据取自每日阅读记录，不额外占空间。
- 结构复查：无重复函数、无失效按钮引用；之前丢的总评（保存在库里、只是没显示的）已可在「阅读记录→我的总评」找回。

## v3.49
- 代码整理：45 份历史更新说明归档到 docs/archive/；统一 v3.N 版本命名；总账并入「总体规划与进度.md」。
- 整理流水线起步：新增「按正文找回/合并作者」——扫正文里的作者标记（微博@/lofter/by/作者：/首发平台）聚类，把同一作者不同笔名归一。管理页新增卡片，**预览优先、执行前自动备份数据库**。

## v3.48
- 手动「理清章节」按钮（解析并存库）；Quill 历史对话（持久化+列表+回看+删除）；拖动作者归类自动改作者名。

## v3.47
- 移动端"加载不全"修复（后台补齐后总是重排）；「安装到主屏/桌面」入口默认可见 + 分平台指引。

## v3.46
- 简评/总评找回（弹窗拉服务端权威值）+ 阅读记录置顶「我的总评」+ 书架最新在读放前；阅读器划线重做（字符偏移定位/就地重绘/长段可划/换色/笔记沿用色）；翻页前后都有动画 + 窄屏适配；搜索与筛选分离 + 漏斗独立一行 + 多选栏深色 + 退出常驻。

> v3.45 及更早的逐批开发笔记原放在 docs/archive/，为精简仓库已移除（其要点均已并入本更新日志）。
